# 更新日志

本文件记录宏泰 AI Agent 对用户可见的功能变化、重要修复和发布边界。结构采用 Keep a Changelog 的“未发布 + 按版本归档”方式，版本名称采用 `MAJOR.MINOR.PATCH` 三段式。

## 版本规则

- 默认只递增第三位补丁版本，例如 `0.1.3` 更新为 `0.1.4`。
- 第一位或第二位版本号只有在产品负责人明确授权后才能调整，不因普通功能或修复自动变化。
- 每个新的 Android 可安装候选都必须递增整数 `versionCode`；相同包名和签名下不得回退或复用旧值。
- `android/app/build.gradle.kts` 是源码候选版本的权威来源；`download.html` 表示已经发布且可下载的当前推荐版本。两者可以在候选验收期间暂时不同，但下载页不得指向不存在或未经发布验收的 APK。
- 新改动先写入“未发布”；确定候选版本时归档到对应版本，端测、签名、SHA-256 和下载地址等事实记录在相应验收文档中。

## [未发布]

## [0.1.27] - 2026-08-30

### 修复

- **智能成片一键制作必报「制作项目不存在或已损坏」**：一句话需求不选参考拆解时，新项目从创建起就被读取校验误判为损坏记录（空 `analysisTaskId` 三层同源问题），管线连 AI 都没调用就失败；现已与「拆解是可选增强」的 v4 设计对齐，一句话可直接开始。
- **新建项目混入旧项目内容**：新建编排期间工作台不再自动选中旧项目；失败兜底不再拉别的项目顶包、不再把旧项目的脚本/配音/报错显示在新页面；迟到落地的一步操作成败也不会写到新页面或把用户拽回去。
- **数字人没有上传入口**：打开「数字人出镜」即出现上传卡片，先传一段数字人预处理 MP4，「一键制作成片」在上传前保持禁用，上传后才启动；不再出现"开了开关却没地方传视频"。
- **删除确认形态不统一**：项目、素材、成片、任务、模板的删除确认全部统一为底部上拉确认弹层（共享 `ConfirmDeleteSheet`），替换原来的内嵌提示区；确认按钮改警示红色并明示「不可恢复」。
- **同一错误一屏重复多遍**：工作台错误只保留内联横幅单面展示，顶部通知与页面错误同生共死、不再赖在下一屏；配音类错误使用简短标题，不再标题正文各复读一遍。
- **排版问题**：预估总时长改单行自然流（不再错位断裂）；页面底部为悬浮主按钮让位，列表最后一行不再被按钮压住；生成中按钮如实显示「正在生成分镜脚本…」；需求占位文案标明「例如：」；模板页不再叠两个空状态。

### 发布边界

- Android 源码版本推进为 `0.1.27` / `versionCode=35`；只生成 Release 产品并按 `HongTai-AI-Agent-release-v0.1.27.apk` 独立归档。
- 本地归档与公网文件均为 23,384,347 字节、SHA-256 `2a5a3a27eb5df11876a7a84f2a26aa2e4116bc4d3d4aa208208d6a4c8361192c`（公网文件已下载回验一致）；Release 单测、lint、四 ABI assemble、16 KiB 对齐、v2/v3 签名、证书锚定（`54df122c...b5b2fde`，与历史发布一致）通过。
- 浏览器真实端测（真实 AI 连接 + 真实 MP4）跑通：一句话/数字人两条一键路径进入流式生成且零「损坏」误报，16/16 功能断言通过、console 零错误；浏览器配音与 Media3 渲染边界如实为错误展示，**物理真机端测仍未完成**。
- `v0.1.27` 已上传公网并完成 `download.html` 公开推荐切换（`https://husteread.com/storage/public/HongTai-AI-Agent-release-v0.1.27.apk`），`v0.1.26` 降为历史版本。

## [0.1.26] - 2026-08-29

### 重构

- 制作板块三种方式（智能成片、爆款复刻、数字人）默认**一键全自动**：创建项目后点「一键制作成片」，分镜脚本 → 逐句配音 → 组装 → 本机合成自动推进，用户只需上传素材；分步编辑保留为逃生口。
- 数字人模式重定义为单段预处理视频：不再要求逐字稿，AI 按每句实测配音时长自动烘焙裁剪/回绕窗口（窗口时长之和与实测时长精确相等），源视频短于配音时回绕拼接并如实提示源偏短；旧「口播切片」项目仅存量保留。
- 分镜脚本生成改流式呈现（骨架句卡 + 流水文本），分镜句新增 AI 自动配置字幕强调词（每句最多 2 词、逐字校验在句内），在字幕 cue 中放大呈现。
- 单镜 300ms 下限与总时长 15–60 秒改为**软违规**提示，不阻塞合成；原创性校验移至脚本生成期，配音完成后不再被合成期阻断。
- 一键流程诚实化：管线失败落盘 `project.issue` 并在界面明确展示原因与软违规提示，不伪装成功或永久“进行中”。

### 发布边界

- Android 源码版本推进为 `0.1.26` / `versionCode=34`；只生成 Release 产品并按 `HongTai-AI-Agent-release-v0.1.26.apk` 独立归档。
- 本地归档与公网文件均为 23,382,674 字节、SHA-256 `1b01fd2a2708da3d4c7da9afdcb4f27e85dbcbc3c8f8e1ba0148945576fd8e11`（公网文件已下载回验一致）；Release 单测、lint、四 ABI assemble、16 KiB 对齐、v2/v3 签名、证书锚定（`54df122c...b5b2fde`，与历史发布一致）和包版本后验通过。
- `v0.1.26` 已上传公网并完成 `download.html` 公开推荐切换（`https://husteread.com/storage/public/HongTai-AI-Agent-release-v0.1.26.apk`）。物理真机端测、真实 Provider 与最终成片全链路未验收。

## [0.1.25] - 2026-08-28

### 重构

- 制作板块按「文稿先行」管线重构：创建只要求一句话需求（来源拆解、主文字、文字预设与贴纸收进高级项），工作台按「需求 → 分镜文稿 → 配音 → 合成 → 成片」阶段推进，每阶段一个产物与一个主按钮；已完成阶段可回改并触发诚实重建，进度随时可见。
- 移除预设视频时长四选一：每镜时长改为对应句实测 TTS 音频时长，总时长超出 15–60 秒软边界时提示回改或确认后继续。
- 分镜卡片支持逐句编辑：改文案、换素材、调贴纸在卡片上就地完成；改了哪句只重新配音该句，其余句子保持已就绪状态，微调页退役为兼容重定向。
- 字幕 cue 边界优先来自实测时间戳：Provider 支持原生时间戳时直用，OpenAI 兼容路径用词级转写反查并如实标注精度差异，不再按字数估算铺满镜头。
- 「数字人」模式改名「口播切片」，能力不变；制作计划升级 `production-plan.v4`，旧 v3 存量项目只读兼容渲染，本地项目不失效。

### 发布边界

- Android 源码版本推进为 `0.1.25` / `versionCode=33`；只生成 Release 产品并按 `HongTai-AI-Agent-release-v0.1.25.apk` 独立归档。
- 本地归档与公网文件均为 23,379,102 字节、SHA-256 `2545ce401283badfbc4b85a782084e7e85ce47469f2f1564ac8c2c9cb73e03e8`（公网文件已下载回验一致）；Release 单测、lint、四 ABI assemble、16 KiB 对齐、v2/v3 签名、证书锚定和包版本后验通过。本轮修复了 `android/local.properties` 因 CRLF 行尾触发 lint `PropertyEscape` 的问题（改为 LF 行尾并清除 lint 中间产物后重新分析）。
- `v0.1.25` 已上传公网并完成 `download.html` 公开推荐切换（`https://husteread.com/storage/public/HongTai-AI-Agent-release-v0.1.25.apk`）。物理真机端测、真实 Provider 与最终成片全链路未验收。

## [0.1.24] - 2026-08-26

### 新增

- 设置「数据」新增本地存储分析页：按任务、观察、制作、模板、缓存和应用数据汇总私有文件的真实占用，媒体与缓存可逐项确认清理；任务、报告、计划、模板、配置等数据文件明确标为不可删除，Keystore 也不伪造可枚举体积。
- 最近拆解与最近观察统一支持长按操作菜单；只有终态记录可以二次确认删除，进行中或未完成记录由界面和运行时服务双重保护。
- `/playbook` 注册最近记录操作组件标本，集中对照长按、取消、二次确认和终态删除边界。

### 修复与优化

- 路由懒加载与页面数据骨架改用同一条导航时间基准；页面不再因为 Suspense 骨架和业务骨架各自等待而连续出现两层懒加载。
- 模板卡片优先显示来源拆解的安全封面；没有封面时使用受控本地视频的首帧降级，图片懒加载、视频只预加载元数据，来源媒体清理后诚实显示不可用。
- 删除观察记录串行化同一会话的报告、追问和删除操作，防止并发请求在删除后重新写回旧报告或消息。
- Release 后验兼容当前 Android Build Tools 的 `apksigner --print-certs` 输出，仍保持正式证书锚定与 v2/v3 校验。

### 发布边界

- Android 源码版本推进为 `0.1.24` / `versionCode=32`；只生成 Release 产品并按 `HongTai-AI-Agent-release-v0.1.24.apk` 独立归档。
- 本地归档为 23,354,343 字节、SHA-256 `53978b4f30de3d0173d2c00876ce0f09c18389894b469a618965c5af36acdd45`；Release 单测、lint、四 ABI assemble、16 KiB 对齐、v2/v3 签名、证书锚定和包版本后验通过。
- `v0.1.24` 已上传到固定公网地址 `https://husteread.com/storage/public/HongTai-AI-Agent-release-v0.1.24.apk`，重新下载后与本地归档逐字节一致；`download.html` 已切换为当前推荐版本。公开分发只证明文件身份和下载链路已回验，不表示真实 Provider、物理真机或最终成片已经验收通过。

## [0.1.23] - 2026-08-26

### 安全加固

- AI 流式正文、非流式响应、原生响应队列和浏览器开发态 AI 流都增加 2 MiB 防护；超限会以稳定可重试错误结束，不把无限响应留在内存里。
- 浏览器开发态不再把 API Key 写入 `secrets.json`；AI 重定向逐跳限制在已配置的 HTTPS 源站，私网字面量和 IPv4-mapped IPv6 地址拒绝访问。
- 浏览器开发态媒体、二进制导入、状态和复制写入先落临时文件再原子替换；Node 页面和媒体读取也增加响应/下载上限，并与 Android/浏览器保持公开 HTTPS 目标边界；Release APK 归档拒绝经过 reparse point 的路径。
- 本地图片观察测试页移除用户文本和模型文本的 HTML 注入 sink；图片 MIME 与文件魔数不一致时在解码前拒绝。

### 稳定性与代码清洗

- 公网任务只持久化可重放的公开 URL 参数，清除跟踪、会话、签名参数和 fragment；诊察追问只在医疗边界 Schema 校验并保存成功后一次性展示。
- 任务、观察报告、微调和复刻详情页统一关闭底部主导航，避免 detail 页面重复挂载 portal 导航和错误底部留白。
- 更新日志页增加官方站点连通性探测；离线 iframe 的伪 `onLoad` 不再遮蔽「官方更新页暂时打不开 / 重新加载」恢复态。
- 增加对应的边界测试、离线/在线更新日志端到端测试和 Release 路径安全回归。

### 发布边界

- Android 版本推进为 `0.1.23` / `versionCode=31`；只生成 Release 产品并按 `HongTai-AI-Agent-release-v0.1.23.apk` 独立归档。
- 本地归档与公网文件均为 23,330,385 字节、SHA-256 `b63e9ab4135d6e4a1ebcb4543e906dc0119e59c7e7e75d61e07a547fab5d8315`；正式签名证书锚点保持不变，主机 Release、v2/v3 和 16 KiB 对齐后验通过。
- `v0.1.23` 已上传到固定公网地址 `https://husteread.com/storage/public/HongTai-AI-Agent-release-v0.1.23.apk`，重新下载并完成大小/SHA-256 回验；`download.html` 现已更新为当前推荐版本。公开分发只证明文件身份和下载链路已回验，不表示真实 Provider、物理真机或最终成片已经验收通过。

## [0.1.22] - 2026-08-21

### 修复

- 到顶或到底再猛拉时，页面不再整页拉伸或跟着晃；上下惯性滚动仍可用。没有上拉刷新；列表仍靠自动更新，失败时才「重新读取」。

### 优化

- 深色模式补全淡绿底、状态软底和观察纸面 token，并把绕过 token 的浅色井改成表面变量；浅色数字不变。系统深色启动帧对齐近黑画布。
- 确认使用图片后立刻进入「AI 正在观察」进行中页（未归档 S9b）：顶部真图持续扫光，下方为运行期深度思考与五模块状态；生成完成后同一地址切到观察报告。取消返回起始页，不宣称已停止 AI。起始页不再挂生成进度卡。
- 观察报告「日常建议」按未归档 S10 收成单列：标题与优先级同一行，说明在下；去掉条目内额外图标列，避免标题被挤成窄竖条。
- 观察报告详情页去掉底栏，底部改为胶囊追问输入；发出问题后上滑「AI 追问」悬浮窗（可拖收起、右上关闭）。宿主底栏改为按路由 `showNav` 关闭，不再强制所有页面常驻。助手回复支持简单 Markdown 渲染，并可复制原始文本；不提供未接通的「有用」。

### 发布边界

- Android 源码版本推进为 `0.1.22` / `versionCode=30`；只生成 Release 产品并按 `HongTai-AI-Agent-release-v0.1.22.apk` 独立归档。
- 本地归档为 23,329,541 字节、SHA-256 `27558531BA4EA77810761EB4A4448865EE142DCCCCB9754CDA2216A6060D4B3E`；证书锚点与既有 Release 身份一致。
- 主机侧 `scripts/build-android-release.ps1` 已通过 Web production build、Capacitor sync、Release JVM、`lintRelease`、四 ABI `assembleRelease`、16 KiB zipalign、包身份与 v2/v3 签名后验。
- 该版本已完成人工上传与公网文件回验；公网文件为 23,329,541 字节、SHA-256 `27558531BA4EA77810761EB4A4448865EE142DCCCCB9754CDA2216A6060D4B3E`，与本地归档一致。仓库 `download.html` 已更新为推荐 `v0.1.22`。公开分发只证明文件身份和下载链路已回验，不表示真实 Provider、物理真机或最终成片已经验收通过。

## [0.1.21] - 2026-08-21

### 新增

- 设置「关于」增加「更新日志」入口：进入后以 iframe 嵌入官方下载与更新站；页内用 `PageSkeleton` 等待站点加载，失败走 `ErrorState`。版本卡下方先列「更新日志 / 设计稿对照」，再展示本机「本版要点」。
- 下载页 `download.html` 不再依赖同目录 `download-brand.png`：顶栏与推荐卡使用当前应用图标的 128px PNG data URI。版本卡高度随内容增高，SHA-256 与标题可断行，避免窄屏/iframe 横向撑破页面。

### 优化

- 统一全应用动效：弹层、骨架、底栏切页、二级页滑入、分段 Tab 指示条共用同一套时长与缓动，不再各写一套。
- 底部选择面板、加号新建与观察确认共用 Overlay 上滑+遮罩，支持点选、拖把手和下滑关闭；底部菜单顶部横杠加大，明确可向下拖关闭。
- 产品路径用页面骨架代替全屏「正在打开页面」转圈；路由懒加载的 fallback 随切页滑入；懒加载与列表骨架至少停留约 400ms，避免一闪而过。
- 底栏四项点击以重叠缓动切到目标页；二级页进入/返回滑入约 720ms；设置子页、任务、观察报告、微调、复刻与加号进制作一律走 `push`，不得瞬间切。左右滑仍走原轨道，提交后不再叠第二次动画。
- 分段 Tab（观察面部/舌象、拆解来源等）点击后指示条与内容按同一套缓动滑动。
- 冷启动开屏补齐产品名与「让 AI 帮你把内容做好」；图标上滑、标语写出、背景光晕。仍停留约 2–3 秒，不是进度条。
- 下载页更新记录改按仓库 `CHANGELOG.md` 写用户能看懂的功能变化；首屏与版本卡视觉向当前页面层靠拢（品牌绿、白底、圆角卡片）。

### 文档

- 新增 [动效规范](docs/动效规范.md)，作为弹层、骨架、路由切换和冷启动开屏的活口径。

### 发布边界

- Android 源码版本推进为 `0.1.21` / `versionCode=29`；只生成 Release 产品并按 `HongTai-AI-Agent-release-v0.1.21.apk` 独立归档。
- 本地归档为 23,323,128 字节、SHA-256 `E95B1A1E0845522BBD25469AC2E6F8F4F42F117BED020C8C15A8E6826102CDFD`；证书锚点与既有 Release 身份一致。
- 主机侧 `scripts/build-android-release.ps1` 已通过 Web production build、Capacitor sync、Release JVM、`lintRelease`、四 ABI `assembleRelease`、16 KiB zipalign、包身份与 v2/v3 签名后验。
- 该版本公开 APK 已完成人工上传与公网文件回验；公网文件为 23,323,128 字节、SHA-256 `E95B1A1E0845522BBD25469AC2E6F8F4F42F117BED020C8C15A8E6826102CDFD`。仓库下载页已推荐 `v0.1.21`；husteread 站点 HTML 同步后，线上推荐口径才与公开文件完全一致。公开分发只证明文件身份和下载链路已回验，不表示真实 Provider、物理真机或最终成片已经验收通过。

## [0.1.20] - 2026-08-21

### 修复

- 加号「智能成片 / 爆款复刻 / 拆解新链接」带查询串导航时不再被当成未知页面；路由只匹配 pathname，查询参数只作为入口意图。这是会直接挡住制作入口的严重回归。
- 拆解首页「上传视频」区按未归档 S2 还原为整卡：浅底圆角、白色视频图标圆与居中说明，不再只剩塌缩的三行文字。

### 文档

- 按当前源码全量校对活规范与 README：`AppRuntime.replica`、制作计划 v2 JSON → 本地组装 v3、九个原生插件、错误码补全、发布无路由。
- 新增 [项目架构解析](docs/项目架构解析.md)，按目录说明每个源文件；2026-08-16 的项目讲解降为历史快照。

### 优化

- 页面层 SVG 改为与未归档稿一致的 Lucide 描边图标；`/playbook` 汇总设计稿板块，供真实页面引用，不读取运行时数据。
- 底栏按主流五格比例收紧：内容区 48px，中间加号改为栏内居中的 32px 方圆钮（`--radius-plus`），与左右图标组同一垂直中线。
- 新建弹层两项图标按 S14 落在 34px 正圆淡绿底上，使用 Lucide `wand-sparkles` 与 `list-checks`。
- 观察采集卡只保留底部拍摄说明，用常规字重的白色荧光字并略上移；四角取景框改为白色；面部/舌象底板叠加镭射扫光。
- 页头用户头像从 32px 微增到 36px，描边改为纯白，上传照片后与页底自然分开。
- 模板页「推荐模板」改为左侧搜索框，右侧仍保留「本机精选 · 滑动查看」，按本机名称、摘要或公式过滤，不编造结果。
- 模板空态说明改为单行显示，避免「从拆解保存」那句折成两行。
- 设置「关于」页的「新版本改了什么」改为说明本版加号入口修复、上传区整卡还原和页面层收口。

### 发布边界

- Android 源码版本推进为 `0.1.20` / `versionCode=28`；只生成 Release 产品并按 `HongTai-AI-Agent-release-v0.1.20.apk` 独立归档。
- 本地归档为 23,311,721 字节、SHA-256 `572D4901F3300615C6C85C3EDBAA766E3DF6E60A23C777DE0D1B08424F6FA0A8`；证书锚点与既有 Release 身份一致。
- 主机侧 `scripts/build-android-release.ps1` 已通过 Web production build、Capacitor sync、Release JVM、`lintRelease`、四 ABI `assembleRelease`、16 KiB zipalign、包身份与 v2/v3 签名后验。
- 该版本已完成人工上传与公网文件回验；公网文件为 23,311,721 字节、SHA-256 `572D4901F3300615C6C85C3EDBAA766E3DF6E60A23C777DE0D1B08424F6FA0A8`。下载页现推荐公开的 `v0.1.20`。公开分发只证明文件身份和下载链路已回验，不表示真实 Provider、物理真机或最终成片已经验收通过。

## [0.1.19] - 2026-08-21

### 新增

- 页面层按未归档 `hongtai-mobile.pen` 落地：观察起始页标题为「AI 诊断」，面部/舌象分段采集，确认选图后才开始会话。
- 观察报告按稿面分区展示，页面顶部和底部都显示免责声明；允许不确定的初步判断，并拦住确诊口吻、概率数字、处方和整体健康评分。
- 设置页接通本机通知开关、深色/浅色/跟随系统、品牌主题色展示、Cache Storage 清缓存和隐私说明。
- 拆解详情增加互动量五格和本地占用；解析到真实数就填，没有就写「未解析到」，不编造热度或缓存体积。

### 优化

- 模板页推荐区改为「本机精选 · 滑动查看」；底栏五项为观察、拆解、加号、模板、设置。
- 抖音、小红书、B 站拆解会带出互动量字段；缺席时界面保持诚实空态。

### 发布边界

- Android 源码版本推进为 `0.1.19` / `versionCode=27`；只生成 Release 产品并按 `HongTai-AI-Agent-release-v0.1.19.apk` 独立归档。
- 本地归档为 23,202,490 字节、SHA-256 `B47A95F68900804C43F15AB2472D598C1E78355BCE53F75A285DF68AA4AAEB1B`；证书锚点与既有 Release 身份一致。
- 主机侧 `scripts/build-android-release.ps1` 已通过 Web production build、Capacitor sync、Release JVM、`lintRelease`、四 ABI `assembleRelease`、16 KiB zipalign、包身份与 v2/v3 签名后验。
- 该版本已完成人工上传与公网文件回验；公网文件为 23,202,490 字节、SHA-256 `B47A95F68900804C43F15AB2472D598C1E78355BCE53F75A285DF68AA4AAEB1B`。下载页现已作为历史版本保留 `v0.1.19`；当前推荐为 `v0.1.20`。公开分发只证明文件身份和下载链路已回验，不表示真实 Provider、物理真机或最终成片已经验收通过。

## [0.1.18] - 2026-08-20

### 修复

- 制作页禁用的主按钮不再挡住卡片上的微调、删除和导入。
- 素材选择器按 Home 再回来时，页面不会永久停在忙碌。
- 应用内切换页面也能结束卡住的规划或合成，不必冷启动。
- 制作计划读不出来时，项目仍可看见、删除或重新生成。
- 数字人合成保留原片声音，不再导出无音轨成片。
- B 站音频流若声明为 `video/mp4`，下载不再被误判失败；HTML/JSON/HLS 仍拒绝。
- 只有导入或渲染时保持屏幕常亮，空闲页恢复系统超时。
- 复刻向导写明云端配音不可用时会回退系统语音，成片仍要回制作页合成。

### 发布边界

- Android 源码版本推进为 `0.1.18` / `versionCode=26`；只生成 Release 产品并按 `HongTai-AI-Agent-release-v0.1.18.apk` 独立归档。
- 本地归档为 21,932,925 字节、SHA-256 `A5D39C07C62A37AF42F70242D17EB3F73C47E05E4BBEEA51759E5A3C931072CF`；证书锚点与既有 Release 身份一致。
- 主机侧 `scripts/build-android-release.ps1` 已通过 Web production build、Capacitor sync、Release JVM、`lintRelease`、四 ABI `assembleRelease`、16 KiB zipalign、包身份与 v2/v3 签名后验。API 35 模拟器上已用归档 `v0.1.17` 以 `adb install --no-streaming -r` 覆盖安装，再同样方式升到 `v0.1.18`，未使用 `-d` 或卸载，`firstInstallTime` 保持 `2026-08-19 17:53:58`。冷启动后首页中文正常。
- 该版本已完成人工上传与公网文件回验；公网文件为 21,932,925 字节、SHA-256 `A5D39C07C62A37AF42F70242D17EB3F73C47E05E4BBEEA51759E5A3C931072CF`。下载页现已作为历史版本保留 `v0.1.18`；当前推荐为 `v0.1.19`。公开分发只证明文件身份和下载链路已回验，不表示真实 Provider、物理真机或最终成片已经验收通过。
- API 35 模拟器上已有 v3 蒙太奇与数字人切片的 Media3 成片证据。物理真机、Agent 真实 `generatePlan` 与 B 站 APK 真网采集仍待用户端测。

## [0.1.17] - 2026-08-20

### 新增

- 制作首页提供 Agent 与爆款复刻两条入口，数字人口播保留在 Agent 流程内。
- 新制作计划使用 `production-plan.v3`，支持五套字幕模板、逐句字幕时间轴、七个随包几何贴纸与真实降级说明。
- 新增卡片式导出微调页，可修改镜头素材、时长、口播、字幕模板、语速、主文字和背景音乐；修改后旧成片会失效并要求重新合成。
- 爆款复刻新增素材需求清单、逐项素材绑定与脚本字幕计划，素材齐备后进入微调页，再回制作页合成。
- Agent 规划前可对导入素材抽帧观察；无法观察时如实降级为盲配，不阻塞制作也不伪造理解结果。

### 修复

- 修复空字幕可能写坏计划、TypeScript 与 Kotlin 毫秒判定不一致、缺贴纸被误报为计划错误等制作链路问题。
- 复刻向导不再把“生成计划”和“本地合成”描述成同一个自动步骤。

### 发布边界

- Android 源码版本推进为 `0.1.17` / `versionCode=25`；正式签名 Release 已按 `HongTai-AI-Agent-release-v0.1.17.apk` 独立归档并上传到固定公网地址。
- 本地归档为 21,932,117 字节、SHA-256 `1358DFA5D16FB6C4F25C2684E4A23B3773296167FE51069CE5B466A1A9212B53`；证书锚点与既有 Release 身份一致。
- 主机侧 Release JVM、`lintRelease`、四 ABI `assembleRelease`、16 KiB zipalign、包身份与签名后验已通过；公网重新下载后的文件大小与 SHA-256 已与本地归档一致。物理真机上的两条制作主链、数字人口播、贴纸与微调烧录仍待用户端测。
- 下载页现已作为历史版本保留 `v0.1.17`；当前推荐为 `v0.1.18`。公开分发只证明文件身份和下载链路已回验，不表示真实 Provider、物理真机或最终成片已经验收通过。

## [0.1.16] - 2026-08-18

### 修复

- 宣称的 Chromium 89 路径不再调用更新年份才有的运行时接口。
- 制作成功落盘后，监听器失败不会把成片改写成失败。
- 深度思考面板与第一块正式内容之间留出缝隙。
- 键盘弹出时输入区会抬高；底栏对系统手势条有 Android 兜底。
- 冷启动改为项目图标加浅色微拟态开屏，停留约 2–3 秒；失败仍有明确终态，不假进度。

### 优化

- 观察、制作、模板、设置改为按需载入；界面中文字体收成子集，首屏不再拉取整份 7.5MB 字体。

### 发布边界

- Android 源码版本推进为 `0.1.16` / `versionCode=24`；只生成 Release 产品并按 `HongTai-AI-Agent-release-v0.1.16.apk` 独立归档。
- 本地归档为 21,829,108 字节、SHA-256 `37580EEDF7587B0C1DF09FC43867DE4029CBF961D2A248E9A05232EC1027F8CA`；证书锚点与既有 Release 身份一致。
- 该版本已完成人工上传与公网文件回验；公网文件为 21,829,108 字节、SHA-256 `37580EEDF7587B0C1DF09FC43867DE4029CBF961D2A248E9A05232EC1027F8CA`。
- 不得把主机测试、浏览器端测或 `assembleRelease` 成功表述为真机键盘、OEM WebView 或 APK 真网已通过。

## [0.1.15] - 2026-08-18

### 修复

- B 站公开链接可以采集到标题、封面和口播；HTTP 412 限流与 `-352` 风控会给出可执行下一步，不再只报含糊错误。
- 成功拆解后可以继续生成制作计划；没有正式拆解或原文时不会假成功。
- 从拆解页进入制作时，即使制作列表第一次读取失败，也不会丢掉要制作的那条内容。

### 发布边界

- Android 源码版本推进为 `0.1.15` / `versionCode=23`；只生成 Release 产品并按 `HongTai-AI-Agent-release-v0.1.15.apk` 独立归档。
- 本地归档为 28,989,075 字节、SHA-256 `75FD98F67918DEF783951B08DD4E5743C9DF6FA2C75A00E8C4C45F739E47E8C0`；证书锚点与既有 Release 身份一致。
- 该版本已完成人工上传与公网文件回验；现已作为历史版本保留，当前推荐为 `v0.1.16`。公网文件为 28,989,075 字节、SHA-256 `75FD98F67918DEF783951B08DD4E5743C9DF6FA2C75A00E8C4C45F739E47E8C0`。
- 不得把主机测试、浏览器端测或 `assembleRelease` 成功表述为真机合成或 APK 真网已通过。

## [0.1.14] - 2026-08-17

### 修复

- B 站链接不再先抓整页 HTML；短链只跟跳转，风控与空播放源会明确失败。真网四类链接仍待验证。
- 本地合成失败拆成可区分错误码，硬件 H.264 失败后可回退软件编码器。真机 logcat 与成片 MIME 仍待验证。
- 模板卡删除按钮在约 390px 下不再被拉成大圆。
- 底栏回到 5 项；富迪素材库改到制作页页头；无行为通知铃和 `/publish` 空路由已去掉。
- 拆解改为首页来源 Tab、单一任务页、原文/拆解分栏；处理中与已完成就地切换。
- 制作页改为 9:16 预览、预览/文案/素材三 Tab，以及按阶段变化的唯一底部主按钮。完成态没有发布入口。
- 启动图标改为 adaptive icon，去掉白底分层。真机桌面遮罩仍待验证。

### 发布边界

- Android 源码版本推进为 `0.1.14` / `versionCode=22`；只生成 Release 产品并按 `HongTai-AI-Agent-release-v0.1.14.apk` 独立归档。
- 本地归档为 28,986,082 字节、SHA-256 `D091EDCAA6B7F09CC3A2DB28364CD8C46719412364A7F03F1E010B12BA8E8579`；证书锚点与既有 Release 身份一致。
- 该版本已完成人工上传与公网文件回验；现已作为历史版本保留，当前推荐为 `v0.1.16`。公网文件为 28,986,082 字节、SHA-256 `D091EDCAA6B7F09CC3A2DB28364CD8C46719412364A7F03F1E010B12BA8E8579`。
- 不得把主机测试、fixture 或 `assembleRelease` 成功表述为真机合成、真网解析或桌面图标已通过。

## [0.1.13] - 2026-08-17

### 修复

- 抖音图文帖可以采集；手机端抖音、B 站链接与适配器判定一致；内容私密或不存在时给出准确提示；解析不确定时明确失败，不再采成推荐视频。
- 观察追问与正式报告都会拦住确诊、处方、概率和健康评分；长对话压缩更稳，失败不再被静默吞掉。
- 任务失败、跳过或进程重建后进入明确终态，不再永久显示等待中；下载或合成失败不会删除已有成功产物。
- 切换制作项目、观察会话和任务详情时，界面只保留当前真实状态，不再串进度或闪回旧数据。
- 采集落盘前去掉签名链接等敏感信息；下载页与探测图改为独立文件，不再内嵌 base64 媒体。

### 发布边界

- Android 源码版本推进为 `0.1.13` / `versionCode=21`；只生成 Release 产品并按 `HongTai-AI-Agent-release-v0.1.13.apk` 独立归档。
- 本地归档为 28,567,582 字节、SHA-256 `1C8AB9484245D75D6EE834716A32FBEBFA328B0C031CDF4C3C13B49D5FCB3C52`；证书锚点与既有 Release 身份一致。
- 该版本已完成人工上传与公网文件回验；现已作为历史版本保留，当前推荐为 `v0.1.16`。公网文件为 28,567,582 字节、SHA-256 `1C8AB9484245D75D6EE834716A32FBEBFA328B0C031CDF4C3C13B49D5FCB3C52`。
- 本版本尚未做物理真机端测，也未配置真实 Provider 端到端；不得表述为已通过真机验证。

## [0.1.12] - 2026-08-15

### 优化

- 清晰舌象与面部图片现在输出 3–6 条独立可见观察；轻微偏暗、角度偏差或局部模糊会继续分析可辨部分，不再轻易判为不可用。
- 深度思考继续显示供应商实时返回内容，但系统提示词将注意力引导到图片证据、特征组合、知识匹配和拍摄干扰，减少无关的 JSON 拼装讨论。
- 舌诊与面诊共用安全、质量和输出底座，并各自使用简短的专属观察重点；传统方向允许 0–3 项，没有组合依据时不强迫生成。

### 修复

- 后续追问不再复用首次报告的 JSON-only 规则，恢复自然中文回答。
- 修复诊察修复提示字段数量错误、Prompt 与 Provider Schema 版本混乱、有限图片固定显示通用质量文案等问题。
- 纯文本格式修复不再重复注入整份知识库，也不会重新发送私人图片。

### 发布边界

- Android 源码版本推进为 `0.1.12` / `versionCode=20`；只生成 Release 产品并按 `HongTai-AI-Agent-release-v0.1.12.apk` 独立归档。
- 该版本已完成人工上传与公网文件回验；现已作为历史版本保留，当前推荐为 `v0.1.13`。公网文件为 28,958,194 字节、SHA-256 `F48C3920113DFF48404F3C6454085AE4F30A7C79F8C0EA3F1334E6258FF408CE`。
- 已公开分发不等于全部门禁通过：真实 Provider 端到端与物理 Android 手机完整端测仍未完成，相关能力不得表述为已通过真机验证。

## [0.1.11] - 2026-08-15

### 新增

- 视频制作新增可选“主文字”输入和三种文字预设；未填写时由 AI 根据用户真实需求生成，填写后制作计划必须逐字使用。
- `production-plan.v2` 增加常驻顶部主文字层，Media3 在每个竖屏镜头同时渲染顶部主文字与底部短字幕。

### 原创性与上下文

- 制作规划现在同时接收来源任务的原始文稿与正式爆款拆解，并在两者之前加入“仅供参考、吸收思路、不得作为本次口播”的明确前缀。
- 原始文稿只在本次规划调用中进行 12,000 字符以内的有界使用，不复制到项目记录或页面 DTO；新口播若与参考原文出现连续 12 字重复，会进入唯一一次计划修复。
- 旧 `production-plan.v1` 继续可读取和渲染，新生成计划统一使用 v2。

### 发布边界

- Android 源码候选推进为 `0.1.11` / `versionCode=19`；只生成 Release 产品并按 `HongTai-AI-Agent-release-v0.1.11.apk` 独立归档。
- 本版本需要完成真实 Media3 instrumentation 合成和 API 35 Release 端测；没有物理 Android 手机证据时仍只能称为 Release 候选。

## [0.1.10] - 2026-08-15

### 新增

- 底部导航新增“富迪素材库”，点击后直接展示用户提供的宣传图；弹层支持点击关闭、关闭按钮和 Esc 键，并保持原有页面滑动顺序不变。

### 视觉更新

- APK 启动图标更换为用户提供的“宏泰AI”机器人图，经过方形裁剪去除来源水印后作为独立 Android launcher 资源；应用页面内原有 SVG、页头标记与其他品牌素材保持不变。
- 富迪素材原图进入 Web `public` 静态资源边界，APK 离线安装后也能直接显示，不依赖外部网址。

### 发布边界

- Android 源码候选推进为 `0.1.10` / `versionCode=18`；只生成 Release 产品并按 `HongTai-AI-Agent-release-v0.1.10.apk` 独立归档。
- 本版本需要完成主机签名构建和 API 35 模拟器弹层端测；没有物理 Android 手机证据时仍只能称为 Release 候选。

## [0.1.9] - 2026-08-15

### 新增

- 舌象与面部观察接入独立的五脏六腑传统观察 Markdown 知识库，覆盖舌质、舌形、舌苔、面部色泽、传统脏腑分区、拍摄干扰与安全转诊边界。
- 观察报告新增“传统望诊参考”：只在图片可用且存在直接可见观察时展示，固定标记为不确定参考和非诊断。

### 安全边界

- 齿痕不再被直接等同于“湿气重”，白苔不再被直接等同于“胃寒”，舌红不再被直接等同于“心火旺”；只有组合特征满足时才给保守的传统关联，并明确单张图片不能据此诊断。
- 继续禁止疾病诊断、器官功能结论、患病概率、处方和整体健康评分；图片不可用时不生成任何传统状态参考。

### 发布边界

- Android 源码候选推进为 `0.1.9` / `versionCode=17`；只生成 Release 产品并按 `HongTai-AI-Agent-release-v0.1.9.apk` 独立归档。
- 本版本需要完成主机签名构建和 API 35 模拟器端测；没有物理 Android 手机证据时仍只能称为 Release 候选。

## [0.1.8] - 2026-08-15

### 修复

- 修复本地视频语音识别失败后只显示汇总错误、掩盖 AI 密钥、模型权限、网络、限流等真实原因的问题；现在会保留可操作的失败提示和对应恢复动作。
- 修复没有有效口播的视频仍继续进入文字拆解、最终以“缺少产物”报错的问题；现在会停止后续拆解并提示重新选择人声清晰的视频。

### 体验优化

- 本地视频入口明确说明当前能力需要带有清晰人声的 MP4，视频只保存在本机；不再暗示静音或纯画面视频也能完成拆解。
- 首页、任务进度、拆解结果、图片观察、应用启动和设置等高频页面改为面向用户的产品语言，减少运行时、Schema、私有目录和状态机等工程术语。
- 顶部通知不再直接展示内部错误码，统一说明“发生了什么、用户可以怎么做”，并在可恢复时提供检查 AI 设置、重试或重新选择等操作。

### 发布边界

- Android 源码候选推进为 `0.1.8` / `versionCode=16`；只生成 Release 产品，并按 `HongTai-AI-Agent-release-v0.1.8.apk` 归档，不生成或交付 Debug APK。
- 每个 Release 版本保存在统一 APK 归档目录；同版本同字节可重复执行，不同字节禁止覆盖，必须推进版本号后重新构建。
- 本版本仍需真实 Android 手机正常覆盖安装并完成本地视频、AI 调用和数据保留端测；主机签名构建成功不等于物理真机或正式公开发布通过。

## [0.1.7] - 2026-08-14

### 变更

- Release 成为唯一的 APK 产品与交付路径；不再构建、交付或归档 Debug APK。
- 构建脚本改为从 `android/app/build.gradle.kts` 这一唯一版本源读取 `versionName` 与 `versionCode` 并核对包身份，不再维护第二份硬编码版本号。
- 通过全部后验后自动保存版本化 APK 到 `output/apk-archive/`；同版本相同字节可重复执行，同版本不同字节拒绝覆盖。

### 说明

- 本版本没有新增业务架构，完整继承 v0.1.6 的单次结构化 AI 生成、深度思考运行期展示、五板块渐显、自动状态更新和视频选择器返回修复。

### 发布边界

- Android 源码候选推进为 `0.1.7` / `versionCode=15`，APK 为 25,955,837 字节、SHA-256 `70A5A11074C94EB9DBC85708158C4E7A57C59AA0390F5D38AB4768A38509952A`。
- 构建时没有连接任何设备，本轮未安装该版本，不声称模拟器或物理真机通过；详见 [v0.1.7 Release-only 命名与 APK 留存验收](docs/验收/2026-08-14-v017-release-archive.md)。

## [0.1.6] - 2026-08-13

### 新增

- 内容拆解、舌象观察和面部观察改为一次紧凑的结构化流式生成；同一响应中的五个顶层板块在分别通过 Zod 和业务语义校验后，逐步向页面提供安全进度投影。
- 新增“深度思考”运行期展示、五板块流光等待、已校验内容渐显、失败板块与后续未开始状态；支持 reduced-motion 和约 390px 移动端布局。
- AppRuntime 契约推进为 `app-runtime.v2`，分析与诊察运行支持按业务 ID 订阅、活动快照重放和 single-flight。

### 变更

- 仓库开发统一回到根项目目录，不再使用 Git worktree 或复制式隔离工作区；Android 只保留 Release APK 产品和交付路径。
- `android/app/build.gradle.kts` 成为 `versionName` 与 `versionCode` 的唯一源码权威，Release 构建脚本动态读取并核对 APK，不再硬编码第二份版本号。
- 正式 `diagnosis-report.v1` 与 `content-analysis.v1` 仍是唯一可保存结果；板块进度与深度思考仅存在于活动运行内存中，退出后不恢复，重试会重新生成完整文档。
- 页面不猜测未闭合 JSON 字段，也不接触供应商响应对象；Provider 仅把推理增量投影成版本化 `thinking` DTO，正式结果、运行审计与本地文件均不保存推理文本或模型原文。
- 正常生成每条 Flow 只调用一次模型；完整文档无效时最多进行一次文本格式修复，诊察修复不会再次携带私有图片。

### 修复

- 任务和报告状态统一为先持久化权威投影与事件、再通知页面，避免通知后立即读取仍得到旧 `running`。
- 首页任务历史、任务详情、内容拆解和观察历史接入事件驱动自动更新；初始列表读取会回放期间到达的 upsert、delete 与报告终态，并拒绝旧请求迟到覆盖，不再要求健康状态下手动刷新。
- 修复系统媒体选择器返回时重载整个 WebView、从而把刚导入的视频及其他运行任务全部标记为中断的问题；选择视频后继续在原页面完成导入。

### 发布边界

- Android 源码候选已推进为 `0.1.6` / `versionCode=14`；签名候选已通过主机后验、API 35 普通覆盖升级、冷启动、真实系统视频选择器取消返回和 instrumentation。既有 code 13 候选已被本修复版取代，不得公开分发或复用。当前公开下载页继续保留已验证的 v0.1.5 / code 12。
- v0.1.6/code14 只形成签名候选，没有完成物理设备完整验收、人工上传、公网 APK 哈希回验和下载页部署，因此从未成为公开推荐版本。

## [0.1.5] - 2026-08-13

### 新增

- 接入官方 `@capacitor/app` 生命周期事件；页面从后台返回时重新读取权威 DTO，并统一对账无法可靠续跑的遗留任务。
- 本地 MP4 系统选择器加入外部 Activity 操作登记，进程或回调丢失后不再留下永久“正在执行中”的任务。

### 变更

- 将 `fix/issue05-issue07` 的受控 Release 签名、Android 7 HEIF fallback、WebView 兼容和云端 TTS 基线，与 `feat/video-workflow-management` 的本地视频拆解、模板和制作管理能力安全合并到同一救援谱系。
- Android 源码候选从已发布测试版 `0.1.4` / `versionCode=11` 单调递增为 `0.1.5` / `versionCode=12`；正式签名 APK、同证书 Release 升级和 API 35 模拟器门禁已通过，物理设备门禁仍未完成。
- 相机继续使用系统相机 Intent 与 FileProvider 临时 URI 授权，相册继续使用系统 Photo Picker/单项文档授权；不新增 `CAMERA`、整库相册或存储危险权限，因此系统设置中没有独立“权限”板块仍可能是正常表现。

### 修复

- 采集、拆解、图片观察、制作规划和渲染在应用返回前台时进入幂等恢复：可重读的状态刷新，不能安全续接的状态转为带稳定错误码和操作建议的明确中断终态。
- 本地视频选择或处理被中断时提示重新选择媒体，而不是误导用户编辑一个已经丢失的输入。
- 生产操作统一进入项目级互斥和运行登记，删除操作不会与规划或渲染并发破坏同一项目。

### 发布边界

- 公开地址中曾出现文件名为 `HongTai-AI-Agent-debug-v0.1.5.apk` 的错误测试包，但其包内真实身份是 `0.0.1` / `versionCode=3` 且使用 Debug 签名，不能覆盖升级 `0.1.4` / `versionCode=11`；下载页已将其标记为撤回并移除安装入口。
- 当前公开 v0.1.4 使用 Debug 证书；Android 已在模拟器上明确拒绝其直接覆盖为团队证书签名的 v0.1.5 Release。正式发布前必须确定允许导出数据的迁移与一次性卸载重装方案，不能用更高 `versionCode` 绕过签名身份。
- `0.1.5` 正式签名 APK 已在 2026-08-13 公开分发，文件为 25,943,725 字节，SHA-256 为 `48D65860532BF1641222173BA42FFE479EA3180B3B75A146357EB44C25D1DE6D`。公开分发事实不等于物理真机验收通过；设备证据边界继续以 [v0.1.5 谱系救援验收记录](docs/验收/2026-08-13-v015-lineage-recovery.md) 为准。

## [0.1.4] - 2026-08-12

### 新增

- AI 拆解支持通过 Android 系统媒体选择器导入本地 MP4，并复用现有转写、七阶段进度和 `content-analysis.v1` 拆解流程。
- 模板管理支持从成功拆解复制模板，以及新建、编辑和删除用户自定义模板。
- 根目录增加版本更新日志，并增加源码候选必须高于已发布下载版本的自动回归约束。

### 变更

- 底部五项导航调整为“AI、拆解、制作、模板、设置”；把已具备真实运行时能力的模板页移入主导航，移除未接入的素材库占位入口。
- 当前源码候选从 `0.1.3` / `versionCode=10` 单调递增到 `0.1.4` / `versionCode=11`。
- `download.html` 继续标记已发布的 `0.1.3`，直到 `0.1.4` 产物完成验收并实际发布，避免生成虚假下载入口。

### 修复

- 视频制作支持删除已导入素材、已生成成片或整个本地制作项目，并在规划或渲染运行期间拒绝破坏性删除。
- 终态内容拆解任务支持显式删除；运行中的任务保持不可删除，避免状态与文件并发失配。
- 本地视频选择器在用户真正选中有效 MP4 前不再创建任务快照，修复取消或外部 Activity 生命周期竞态可能留下空任务的问题。
- 恢复以真实 `0.1.3` Android 应用分支为父版本，修复从未合入该分支的旧 `main` 开发导致的版本回退风险。

### 验证边界

- 当前条目描述源码候选，不代表已经更新公开下载页或完成正式分发。
- 物理 Android 设备上的系统媒体选择、Media3 合成、删除和无降级升级仍必须以真实设备证据为准。

## [0.1.3] - 2026-08-11

### 修复

- 将标准 Android WebView 启动基线收敛到 Chromium 89，并为 Huawei provider 使用独立兼容基线，避免合法设备被永久拦截。

### 验证边界

- 当前公开下载页推荐版本为 `0.1.3`，Debug APK 的历史 SHA-256 为 `AE536C5CF6620CB902CEA8D9A63B3CDDF3A7E884E26604C1C0272DAAEE9D0D16`。

## [0.1.2] - 2026-08-11

### 新增

- 增加小米 MiMo、阶跃星辰的一键 AI 配置，并扩展云端 TTS 参数与安全探测边界。

## [0.1.1] - 2026-08-11

### 变更

- 完成界面可读性、系统 TTS 入口和应用版本信息展示优化。

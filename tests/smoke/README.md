# 手动冒烟验证

后续为抖音、小红书、B站各保留少量公开测试链接及人工运行说明。快手另按实验性标准保留至少两个短链和一个标准作品链接，并分两个时间窗口验证；测试链接不提交到仓库。

真实网络测试不作为每次本地检查的强制条件。

快手人工运行命令：

```powershell
pnpm cli ingest "<自己有权处理的快手公开链接>"
```

成功时检查MP4下载与FFprobe；触发验证码时应返回`PLATFORM_RISK_CONTROLLED`且CLI不崩溃。任何结果都不得在任务日志和`raw/response.json`中保留CDN签名参数。

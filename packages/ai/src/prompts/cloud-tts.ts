/** MiMo chat-audio user-turn instruction. Native writes this into the vendor body as-is. */
export const MIMO_CHAT_AUDIO_TTS_INSTRUCTION = "请以自然、清晰的普通话播报。";

/** StepFun audio-speech `instruction` field. Native writes this into the vendor body as-is. */
export const STEPFUN_AUDIO_SPEECH_TTS_INSTRUCTION = "自然、清晰的中文视频旁白。";

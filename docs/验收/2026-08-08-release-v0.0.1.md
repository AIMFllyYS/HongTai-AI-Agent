# HongTai AI Agent release v0.0.1

> **历史 QA 记录，不是正式 release 声明。** 本文中的 APK 由 Debug 证书签名，且 `versionCode=1` 存在正常升级风险；不得据此对外分发。当前发布门禁请见[当前能力与发布状态](../当前能力与发布状态.md)。

日期：2026-08-08

## APK

- 文件：`android/app/build/outputs/apk/release/app-release.apk`
- applicationId：`com.hongtai.aiagent`
- versionCode：`1`
- versionName：`0.0.1`
- SHA-256：`6CE86479262FD5FC28A6A385AD2AA298C7E0C2D4E9A308D381D0BDD1B53E04AD`
- 未签名中间产物：`app-release-unsigned.apk`

## 图标

Android 启动图标直接复用设计稿完整资产：

`apps/web/public/brand/pulse-flow-source.png`

该文件被复制到 `android/app/src/main/res/drawable-nodpi/pulse_flow_launcher.png`，并由 Manifest 的 `android:icon` 与 `android:roundIcon` 使用。没有重新绘制、裁切或替换成临时图标。

## 签名边界

仓库没有生产发布 keystore，因此本 APK 使用本机 Android 默认 debug keystore 做 QA 签名，已通过 APK Signature Scheme v2/v3 校验并可安装到测试设备。该签名不能作为 Play 商店或正式分发签名；正式发布前必须替换为团队保管的 release keystore，并重新生成 SHA-256。

## 验证

- `pnpm check`：178/178 通过。
- `android/gradlew.bat :app:testReleaseUnitTest --no-daemon`：通过。
- `android/gradlew.bat :app:assembleRelease --no-daemon`：通过。
- `aapt2 dump badging`：确认 `versionCode=1`、`versionName=0.0.1`，图标资源已进入 APK。
- `adb install -r -d app-release.apk`：安装成功；包管理器确认 `versionCode=1`、`versionName=0.0.1`。

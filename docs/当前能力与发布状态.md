# 当前能力与发布状态

> 最后校对：2026-08-26。本文是能力和发布成熟度的当前来源；日期验收文档仅保存当时证据。

## 一句话结论

完整功能源码已经统一进入仓库根目录。AI 采用“一次视觉生成、最多一次纯文本格式修复、五个已校验展示板块、运行期真实深度思考”；舌象与面部观察共用分层 Prompt 和独立 Markdown 知识库。制作板块源码现有 Agent、爆款复刻与 Agent 内的数字人口播，生成计划使用 `production-plan.v3`，支持字幕模板、逐句时间轴、随包贴纸和卡片式导出微调。Android Media3 只执行通过校验的计划。2026-08-20 在 API 35 x86_64 AVD 上，签名 Release instrumentation 已用真实 H.264/AAC 竖屏夹具产出带 cues、贴纸和浮动文字的蒙太奇成片，以及保留原声的数字人切片成片；这是模拟器 Media3 证据，不是物理真机通过。项目只使用根项目目录和 Release APK 交付路径，版本唯一来源是 `android/app/build.gradle.kts`；构建成功后按版本号分别归档，不覆盖旧 APK。当前公开推荐与源码版本均为 `v0.1.23`/`versionCode=31`，已完成本地 Release 签名、公网上传、逐字节哈希回验和 `download.html` 同步。物理设备上的制作主链仍待用户端测，因此不能表述为真机通过。

## 运行时能力矩阵

以下能力来自 `packages/capacitor-runtime/src/standalone-app-runtime.ts` 的 `FeatureCapabilityRegistry`。`available` 表示运行时已接入，不等同于所有设备、所有网络或正式分发均已验收。

| 能力 | 状态 | 用户可做的事 | 重要边界 |
| --- | --- | --- | --- |
| 本地档案 | available | 编辑显示名、门店信息与私有头像 | 无登录、无云同步 |
| AI 设置 | available | 选择小米 MiMo/阶跃星辰并只填写 API Key，或使用高级 OpenAI 兼容配置；分别探测文本、视觉、ASR、视频配音 | Key 只进入 Android Keystore；页面不读取原始响应或音频 |
| URL 采集 | available | 处理公开单条作品并显示七阶段真实事件；终态任务可明确删除 | 不处理 Cookie、登录、批量采集或绕过风控；运行中任务不可删除。B 站解析不再为 parse 拉整页 HTML；`-352` 映射为 `PLATFORM_RISK_CONTROLLED`，HTTP `412` 映射为可重试的 `PLATFORM_API_RATE_LIMITED`。2026-08-18 在浏览器 Vite 开发态用公开稿 `https://www.bilibili.com/video/BV1bybQ6KEQP`（54 秒）跑通采集：任务 `succeeded`、`speechStatus=transcribed`、`issues=[]`。这不是 APK/真机证据。APK 上 B 站解析成功后单视频下载仍待真网复验，见 [#122](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/122)；超过默认时长上限的长稿仍会失败 |
| 内容拆解 | available | 基于真实转写或图文证据运行 `content-analysis.v1`；带清晰口播的本地 MP4 复用同一七阶段、语音识别与正式拆解 | 公网任务仍由用户确认后拆解；本地视频选择后自动拆解。没有可用口播时明确提示更换视频，不再继续进入文字拆解；当前不分析静音视频画面，也不伪造证据 |
| 图片观察 | available | 选择舌象或面部单张图片、生成观察报告并追问；确认后进入「AI 正在观察」进行中页，完成后同一地址展示报告；最近观察支持长按，终态记录可二次确认后删除 | 页名「AI 智能诊断」；进行中页无体征数字或健康评分；先描述可见证据，再给需医院核实的不确定初步判断；不是确诊、器官功能判断、处方、概率或健康评分；`pending/running` 观察由 `deleteSession` 再次保护 |
| 本地存储分析 | available | 从设置进入存储分析页，查看应用私有目录的实际文件大小、分组占用和可清理项目；媒体/缓存可逐项确认删除 | 路径和原生 URI 不出端；任务、报告、计划、模板和配置数据文件始终标记为不可删除；Android Keystore 不提供可枚举文件大小 |
| AI 运行反馈 | available | 在内容拆解、面部观察、舌象观察期间展开查看实时“深度思考”，并依次看到五个固定展示板块；诊察思考被要求聚焦图片证据、知识组合与拍摄干扰 | 深度思考只存在于当前运行内存，不保存、不写日志、不进入追问；页面不读取供应商响应对象、不展示半截 JSON，正式报告仍以完整 Schema 校验和原子保存为准 |
| 本地视频制作 | available | 制作首页先选 Agent 模式或爆款复刻。Agent 基于成功拆解导入图片/视频素材；制作时可填写顶部主文字或交由 AI 生成，并选择经典顶部白字、白色卡片或青绿强调三种预设；默认写旁白和底部字幕，配音优先用 AI 连接中的云端旁白，旧连接未配云端 TTS 才回退系统语音。数字人口播是 Agent 流程内的二次选项：导入带原声 MP4 并按用户口播稿生成字幕。爆款复刻选一条成功拆解后进入 `/replica/:taskId` 向导。可删除素材、成片或整个项目。制作页为 9:16 预览、三 Tab 与阶段化唯一主按钮 | 爆款原文与拆解只在规划请求内作为受限参考，不持久化原文；较长连续重复会触发一次修复。后台返回可终止遗留 `planning/rendering`，但不后台续跑；删除素材会使旧计划与成片失效，规划或渲染期间拒绝删除。导出失败已拆成可区分错误码并允许软件 H.264 回退。字幕出入点目前只有 `script_estimate`（按字数铺满镜头），不是对着语音测出来的。旁白会参考画面里看得见的内容，但**没有**机器核对旁白是否对得上画面，也没有「清单要的」与「画面有的」比对；界面要求逐镜核对。2026-08-18 浏览器已把「成功拆解 → 添加 3 张素材 → AI 生成制作计划」跑通：`project.status=ready`、`plan.schemaVersion=production-plan.v2`；无正式拆解或无原文时 `generatePlan` 必须落成 `failed` 且不写 plan，测试已锁。浏览器 `render` 故意抛 `ERR_MEDIA_ENCODER_UNAVAILABLE`，不得表述为 Media3/真机合成通过。2026-08-20 API 35 模拟器 Release instrumentation 已用真实 MP4 夹具产出 v3 蒙太奇与数字人切片成片（H.264 + AAC，显示 720×1280），详见 [批次 11 Media3 验收](验收/2026-08-20-batch11-media3.md)；物理真机与 Agent 真实 `generatePlan` 仍未验。云端 TTS 与 OEM 媒体实现仍需另验 |
| 模板管理 | available | 从成功拆解复制结构，或新建、编辑、删除本地模板；有来源任务时显示真实解析视频封面或受控视频首帧 | 模板不复制原视频；页面只读取来源任务的安全 `content.cover` 投影，来源媒体已清理时诚实显示不可用；不做云模板市场 |
| 发布 | planned | 无界面入口；`/publish` 已移出路由表 | 不唤起平台、不伪造发布成功 |

AI 运行反馈是两条简单 AI Flow，不是 Agent、工具调用循环或动态编排器。诊察与内容拆解在正常路径各发出一次结构化流式请求；同一响应中的五个顶层字段或字段组完整闭合后，先通过对应 Zod 与业务语义校验，再发送 `structured-generation-progress.v1` 安全投影。完整文档无效时最多进行一次格式修复，诊察修复为纯文本且不再次发送私有图片。板块进度和推理文本都不持久化；WebView 或进程退出后不恢复半成品，用户重试会重新生成完整文档，只有完整 `diagnosis-report.v1` / `content-analysis.v1` 校验并原子保存后才成立。

任务列表、任务详情、内容拆解与观察报告在健康状态下通过按 ID 或窄变更事件自动更新。权威记录先落盘再通知页面；初始异步列表读取会回放读取期间到达的 upsert、delete 或报告终态，并拒绝更早请求的迟到结果，因此不需要固定轮询或手动刷新。只有读取或订阅失败时保留“重新读取”恢复动作。

## 发布状态

| 项目 | 当前事实 | 结论 |
| --- | --- | --- |
| 版本谱系 | 源码与公开推荐均为 `versionCode=31`、`versionName=0.1.23`；本地归档和公网文件 `HongTai-AI-Agent-release-v0.1.23.apk` 均为 23,330,385 字节、SHA-256 `b63e9ab4135d6e4a1ebcb4543e906dc0119e59c7e7e75d61e07a547fab5d8315`，公网地址为 `https://husteread.com/storage/public/HongTai-AI-Agent-release-v0.1.23.apk`。`v0.1.22`/code30 及更早 Release APK 仍分别保存在 `output/apk-archive/`，同版本不同字节不得覆盖 | 0.1.23 已通过主机 Release 签名、归档、公网上传和逐字节哈希后验，并已同步 `download.html`；物理真机端测仍未完成 |
| 错误同名 APK | 公开的 `HongTai-AI-Agent-debug-v0.1.5.apk` 已核对为 16,491,092 字节、SHA-256 `9ffb4b8eb4ef6b67fc8c13dd5f8d49d05eac81e6b9066c5832f83cb9d44e43a7`，但包内真实身份为 `0.0.1`/3 且为 Debug 签名 | 它不是本救援源码的产物，不能升级 `v0.1.4`/11；下载页已移除其安装入口，禁止覆盖文件后沿用旧哈希或宣称正式版 |
| Release 签名 | 当前公开版本 `v0.1.23`/code31 已从仓库外签名配置在根目录重新构建；APK non-debuggable，16 KiB zipalign、v2/v3、四 ABI、包身份与证书锚点全部通过。证书 SHA-256 为 `54df122cd4f99720c613737815385e771bfaeb17715c160aed178062ab5b2fde`；公网文件与本地归档哈希一致 | 正式 Release 身份连续；公开文件已回验，物理真机仍未完成 |
| 图片兼容 | minSdk 24；当前 HEAD 的单张 HEVC HEIF/HEIC native fallback 已在 API 24/25 独立 x86_64 AVD 分别通过 6/6 instrumentation。WebView 53/55 上的 v7 低版本中文终态页已复验；当前标准 Android provider 的启动基线为 Chromium 89，Huawei provider 采用独立版本基线 10，不再按不可达到值 fail-closed。API 35 WebView 124 上，v7 首页、观察页及真实 Photo Picker 的平台可解码 HEIC、PNG 正向路径通过；普通 Photo Picker 会在 URI 到达应用前过滤本轮损坏 HEIC，故不声明 API 35 UI `IMAGE_INVALID` 已复验 | [#6](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/6) 已完成原生 fallback 与 v7 分层模拟器端测；Android 7.x 同设备完整 UI 闭环、物理设备、ARM 与 OEM HEIC 仍未验证；不支持 AVIF/序列 |
| 设备证据 | code17 和 code18 在 API 35 / Android 15 / x86_64 只读模拟器分别完成同证书普通覆盖升级；code19 的 Release instrumentation 实际生成 15 秒、720×1280 显示方向、顶部主文字和底部字幕并存的 H.264/AAC MP4。code20 归档 APK 以 `adb install --no-streaming -r` 从已安装 code13 普通覆盖升级，未使用降级参数或卸载，`firstInstallTime` 保持不变。2026-08-20 同一 AVD 上先装归档 `v0.1.17`/code25，再以同样命令覆盖安装归档 `v0.1.18`/code26，未使用 `-d`，`firstInstallTime=2026-08-19 17:53:58` 保持不变；冷启动后首页中文、底栏与空任务态正常。同日 Release instrumentation 用真实竖屏 MP4 夹具产出 `production-plan.v3` cues/贴纸/浮动文字与数字人顺序切片；成片哈希与抽帧见 [批次 11 Media3 验收](验收/2026-08-20-batch11-media3.md)，覆盖升级见 [v0.1.18 候选验收](验收/2026-08-20-v018-release-candidate.md) | 这是 Android WebView、页面与模拟器 Media3 / 覆盖升级证据；物理手机的 launcher 缩放、屏幕色彩、真实触控、OEM WebView 与模型端到端仍未验证 |
| 后台返回与异常终态 | 官方 `@capacitor/app` 已接入；系统媒体选择属于受控外部 Activity，选择器正常返回时只关闭对应登记并继续原 WebView，不再触发全局恢复与任务中断。只有进程确实重建或执行失联时，采集、拆解、观察和制作任务才进入幂等中断恢复 | 这是“保留正常返回、可靠发现真实失联”，不是后台续跑；尚需物理机和真实长任务覆盖 OEM 杀进程、选择大 MP4 与恢复提示 |
| 相机/相册权限 | 最终 Release 清单实读只有 INTERNET、ACCESS_NETWORK_STATE、WAKE_LOCK 和 Android 自动生成的非导出 receiver 权限；相机走外部 Intent + FileProvider 单次授权，相册走 Photo Picker/单项 URI，模拟器已验证两个系统 Activity 可打开和取消返回 | 不声明 `CAMERA`、`READ_MEDIA_IMAGES`、`READ_EXTERNAL_STORAGE` 或 `MANAGE_EXTERNAL_STORAGE`，所以系统设置可能没有“权限”板块，这是预期；未来改用 CameraX 才需独立申请 `CAMERA` |
| 链接解析诊断 | #3 的原生失败分类、安全 DTO、持久化映射和通知展示已合并并关单 | 真实断网、DNS、TLS、切网与物理机网络 E2E 仍未做，不再把已关闭的 #3 当作打开的验收 Issue |
| CLI 图片解码安全 | Node 本地图片解码已精确升级到 `sharp 0.35.3`；本版本在解码前检查 JPEG/PNG/WebP 魔数，拒绝声明 MIME 与实际格式不一致的 GIF/TIFF 等输入；页面响应和媒体下载有明确上限，Node HTTPS 目标与 Android/浏览器一致拒绝本机/私网字面量；`pnpm audit --prod` 无已知生产依赖漏洞 | Windows x64 CLI 端测与本轮边界测试已完成；Linux/macOS 未验证，APK 不依赖 sharp。[#7](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/7) 的代码缺口已在本版本落地，但 GitHub Issue 未由本任务代关闭 |
| Android 质量门禁 | `v0.1.23`/code31 已通过 `scripts/build-android-release.ps1`：Web production build、Capacitor sync、Release JVM、`lintRelease`、四 ABI `assembleRelease`、包身份、正式签名、16 KiB zipalign、本地 SHA-256 与公网 SHA-256 后验。公开归档为 23,330,385 字节、SHA-256 `b63e9ab4135d6e4a1ebcb4543e906dc0119e59c7e7e75d61e07a547fab5d8315`；详见 [v0.1.23 Release 验收](验收/2026-08-26-v023-release-candidate.md) | 0.1.23 已公开分发且下载页已同步；仍未连接物理设备，不能声称真机制作、真实 Provider 或物理机覆盖升级 |

## 当前修复队列

当前队列以 GitHub **open 且无 `future`** 为准。已完成批次的实现细节见对应[任务](任务/)与[验收](验收/)，不在本节重复。源码地图见[项目架构解析](项目架构解析.md)。

**不要把下面已合入的代码写成门禁已过：** 物理真机全链路、Agent 真实 `generatePlan`、B 站 APK 真网采集仍未验收。这些是发布成熟度边界，不是把已关闭 Issue 重新打开的理由。

### 当前（open、无 future）

- [#94](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/94) 整体前端按 `docs/Hong/新设计/hongtai-mobile.pen` **未归档**画板做页面层 1:1（当前分支 `feat/hongtai-mobile-ui-redesign`）。
- [#122](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/122) APK 上 B 站解析成功后单视频下载立即失败。源码已接受 DASH 音频槽常见的 `video/mp4` 声明，**APK 真网未复验**。

### 已打 `future`（问题仍在，本轮不排期）

- 字幕下一层：旁白 TTS 实测时长 [#115](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/115)、词级 `asr_word` [#116](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/116)。
- 制作韧性：文件层原子交换 [#118](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/118)；「清单要的」与「画面有的」比对 [#120](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/120)。
- 观察产品：舌象/面部五行全身参考 [#121](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/121)。
- 产品方向：数字人对标剪映 [#97](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/97)；Capacitor 8.x 与真实安全区 [#128](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/128)。
- 2026-08-16 转 future 的 #7（危险解码器魔数预检）与 #8（harness 安全 DOM）已在本版本实现并有边界测试；GitHub 状态不由本任务代关闭。其余 #15–#19、#21、#23–#28 仍按 future 保留。

### 已关闭批次（不要再当待办）

- #1–#6、#9–#14、#22、#29 以及批次 0–7（#30–#77）已在 2026-08-16 关闭。
- 批次 8 [#85](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/85)：可用性阻断与交互结构已关；B 站下载剩余见 #122。
- 批次 9 [#98](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/98)：Chromium 89、监听器、开屏、懒加载、键盘 inset 已关；24px 安全区兜底见 #128。
- 批次 10 [#103](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/103)：字幕模板、v3 计划、烧录、微调、复刻向导、insight、装饰与双模式入口已关；后续能力见 #115 #116 #118 #120。
- 批次 11 [#123](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/123)：制作页按钮、选择器 resume、SPA 恢复已关。模拟器 Media3 见 [批次 11 验收](验收/2026-08-20-batch11-media3.md)。当前公开推荐安装包为 `v0.1.23`/code31；`v0.1.22`/code30 已作为历史版本保留，见 [v0.1.23 Release 验收](验收/2026-08-26-v023-release-candidate.md)。

## 最小验证基线

每次变更按影响范围选择验证，详情见[任务执行模板](任务执行模板.md)：

- TypeScript：`pnpm check` 与定向测试；
- Web：`pnpm --filter @hongtai/web build`，视觉改动补桌面与约 390px 截图；
- Android：相关 Gradle 单测、lint、构建；外部媒体、网络和 Media3 改动补真机证据；
- Release：签名、递增版本号、APK SHA-256 和不带降级参数的升级测试。

不要把“构建成功”“模拟器通过”或“某一个 AI 探测成功”表述为正式 release、物理真机或全部模型能力均已验证。

# 当前能力与发布状态

> 最后校对：2026-08-13。本文是能力和发布成熟度的当前来源；日期验收文档仅保存当时证据。

## 一句话结论

APK 已具备本地档案、AI 设置、公开链接采集、本地 MP4 上传自动拆解、结构化运行反馈、图片观察、可编辑模板与本地视频制作管理链路；制作素材、成片、项目和终态拆解任务均可明确删除。后台返回会对账并终止无法可靠续跑的遗留状态，但仍不承诺后台持续执行。救援分支已同时恢复受控 Release 签名工程、Android 7 HEIF fallback、WebView 89/Huawei 10 基线、云端 TTS 和视频工作流；`v0.1.5`/`versionCode=12` 已完成主机 Release 构建、签名后验和 API 35 模拟器门禁，并已作为正式签名 APK 公开分发。公开分发事实不等于物理真机验收通过：当前仍没有物理设备证据，公开 v0.1.4 的 Debug 证书也不能直接覆盖升级到团队 Release 证书。

## 运行时能力矩阵

以下能力来自 `packages/capacitor-runtime/src/standalone-app-runtime.ts` 的 `FeatureCapabilityRegistry`。`available` 表示运行时已接入，不等同于所有设备、所有网络或正式分发均已验收。

| 能力 | 状态 | 用户可做的事 | 重要边界 |
| --- | --- | --- | --- |
| 本地档案 | available | 编辑显示名、门店信息与私有头像 | 无登录、无云同步 |
| AI 设置 | available | 选择小米 MiMo/阶跃星辰并只填写 API Key，或使用高级 OpenAI 兼容配置；分别探测文本、视觉、ASR、视频配音 | Key 只进入 Android Keystore；页面不读取原始响应或音频 |
| URL 采集 | available | 处理公开单条作品并显示七阶段真实事件；终态任务可明确删除 | 不处理 Cookie、登录、批量采集或绕过风控；运行中任务不可删除 |
| 内容拆解 | available | 基于真实转写或图文证据运行 `content-analysis.v1`；本地 MP4 复用同一七阶段、ASR 与正式拆解 | 公网任务仍由用户确认后拆解；本地视频选择后自动拆解；两者都不伪造证据 |
| 图片观察 | available | 选择舌象或面部单张图片、生成观察报告并追问 | 仅日常参考，不是医疗诊断 |
| AI 运行反馈 | available | 在内容拆解、面部观察、舌象观察期间查看真实请求阶段、已接收内容量和已识别段落 | 只展示安全摘要；最终报告仍以持久化 Schema 为准，不展示 reasoning 或原始 Provider 响应 |
| 本地视频制作 | available | 基于成功拆解导入图片/视频素材；“素材剪辑 + TTS”使用 AI 连接中的云端旁白和字幕，旧连接未配云端 TTS 才回退系统语音；“数字人口播”导入带原声 MP4 并按用户口播稿生成字幕；可删除素材、成片或整个项目 | 后台返回可终止遗留 `planning/rendering`，但不后台续跑；删除素材会使旧计划与成片失效，规划或渲染期间拒绝删除；云端 TTS、编码器和 OEM 媒体实现仍需真机覆盖 |
| 模板管理 | available | 从成功拆解复制结构，或新建、编辑、删除本地模板 | 模板不复制原视频、供应商响应或 reasoning；不做云模板市场 |
| 发布 | planned | 显示明确的未接入状态 | 不唤起平台、不伪造发布成功 |

## 发布状态

| 项目 | 当前事实 | 结论 |
| --- | --- | --- |
| 版本谱系 | 当前公开推荐版本为 `versionCode=12`、`versionName=0.1.5`。正式签名 APK 为 25,943,725 字节、SHA-256 `48d65860532bf1641222173ba42ffe479ea3180b3b75a146357eb44c25d1de6d`；Debug QA APK 为 39,095,463 字节、SHA-256 `278c3cb523eafadd08cde66d5ce1033c6f4768626037c6afe55286b7ca0b41bd` | v0.1.5 已公开分发；物理真机仍未验收，错误同名 APK 不得复用 |
| 错误同名 APK | 公开的 `HongTai-AI-Agent-debug-v0.1.5.apk` 已核对为 16,491,092 字节、SHA-256 `9ffb4b8eb4ef6b67fc8c13dd5f8d49d05eac81e6b9066c5832f83cb9d44e43a7`，但包内真实身份为 `0.0.1`/3 且为 Debug 签名 | 它不是本救援源码的产物，不能升级 `v0.1.4`/11；下载页已移除其安装入口，禁止覆盖文件后沿用旧哈希或宣称正式版 |
| Release 签名 | v0.1.5/code12 已从仓库外 properties 重新构建；APK 为 25,943,725 字节、SHA-256 `48d65860532bf1641222173ba42ffe479ea3180b3b75a146357eb44c25d1de6d`，non-debuggable，zipalign 16 KiB 与 v2/v3 通过，四 ABI 完整；证书 SHA-256 为 `54df122cd4f99720c613737815385e771bfaeb17715c160aed178062ab5b2fde`。API 35 模拟器同证书 v3→v12 普通升级成功并保持 `firstInstallTime` | 正式 Release 谱系本身连续；但公开 v0.1.4 是 Debug 证书，覆盖正式 APK 被 Android 以 `INSTALL_FAILED_UPDATE_INCOMPATIBLE` 拒绝。进入正式谱系前必须制定数据迁移与卸载重装方案，且仍需物理真机门禁 |
| 图片兼容 | minSdk 24；当前 HEAD 的单张 HEVC HEIF/HEIC native fallback 已在 API 24/25 独立 x86_64 AVD 分别通过 6/6 instrumentation。WebView 53/55 上的 v7 低版本中文终态页已复验；当前标准 Android provider 的启动基线为 Chromium 89，Huawei provider 采用独立版本基线 10，不再按不可达到值 fail-closed。API 35 WebView 124 上，v7 首页、观察页及真实 Photo Picker 的平台可解码 HEIC、PNG 正向路径通过；普通 Photo Picker 会在 URI 到达应用前过滤本轮损坏 HEIC，故不声明 API 35 UI `IMAGE_INVALID` 已复验 | [#6](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/6) 已完成原生 fallback 与 v7 分层模拟器端测；Android 7.x 同设备完整 UI 闭环、物理设备、ARM 与 OEM HEIC 仍未验证；不支持 AVIF/序列 |
| 设备证据 | API 35 x86_64 模拟器已对最终 Release 验证系统相机、Photo Picker 和 MP4 Picker 真实打开及取消返回；三次均回到原 Activity、保持 PID、无致命异常。connected instrumentation 为 7 项、0 failure、2 个仅适用于 API 24/25 的 skip，Media3 确定性渲染通过 | 没有物理设备；真实图片/MP4、有效 AI Key、云端 TTS、OEM 编码器和删除全链路仍未完成本轮物理真机验收，模拟器证据不能替代真机 |
| 后台返回与异常终态 | 官方 `@capacitor/app` 已接入；本地 MP4 系统选择器进入统一外部 Activity 登记，运行中公网采集与排队/运行中的本地视频采集、拆解、观察报告、制作计划/渲染均有幂等中断恢复，页面在安全返回时重读 DTO；本地视频中断提示会引导重新选择媒体。最终 Release 在模拟器上 Home 往返保持进程，受控杀进程后以新 PID 干净重建且首页恢复 | 这是“可靠发现并降级”，不是后台续跑；尚未用物理机和真实长任务覆盖最小化、OEM 杀进程与恢复提示，范围见 [v0.1.5 验收记录](验收/2026-08-13-v015-lineage-recovery.md) |
| 相机/相册权限 | 最终 Release 清单实读只有 INTERNET、ACCESS_NETWORK_STATE、WAKE_LOCK 和 Android 自动生成的非导出 receiver 权限；相机走外部 Intent + FileProvider 单次授权，相册走 Photo Picker/单项 URI，模拟器已验证两个系统 Activity 可打开和取消返回 | 不声明 `CAMERA`、`READ_MEDIA_IMAGES`、`READ_EXTERNAL_STORAGE` 或 `MANAGE_EXTERNAL_STORAGE`，所以系统设置可能没有“权限”板块，这是预期；未来改用 CameraX 才需独立申请 `CAMERA` |
| 链接解析诊断 | #3 的原生失败分类、安全 DTO、持久化映射和通知展示已合并 | 真实断网、DNS、TLS、切网与物理机网络 E2E 仍需按 [#3](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/3) 验收 |
| CLI 图片解码安全 | #7 已将 Node 本地图片解码精确升级到 `sharp 0.35.3`；Windows x64 fresh frozen install 实际加载 libvips `8.18.3`，真实 CLI 回环 HTTPS Provider/HTTP API 的方向修正、2048 限边、稳定失败与请求级超限契约通过，2026-08-10 的 `pnpm audit --prod` 为 0 个已知漏洞，原 clean 工作树 `pnpm check` 为 218/218 | Windows x64 CLI 端测与统一检查已完成，spec 与 quality 均已批准；Linux/macOS 未验证，APK 不依赖 sharp |
| Android 质量门禁 | 最终候选已通过独立 `pnpm test` 245/245、`pnpm check`、626-module Web production build、Debug/Release JVM 各 70/70、Debug/Release lint 各 0 error/23 warning、Debug 与 instrumentation 构建、API 35 connected tests，以及正式 Release 构建/包身份/签名/哈希后验 | 主机与模拟器门禁通过不等于物理真机或正式发布通过；签名迁移、真实网络、云端旁白、真实媒体与 OEM 行为仍按 [v0.1.5 验收记录](验收/2026-08-13-v015-lineage-recovery.md) 阻断 |

## 当前修复队列

优先级标签和完整验收条件在 GitHub Issue 中维护：

- P0：#1 外部图片选择恢复、#2 图片主线程导入、#3 链接失败诊断、#4 版本升级身份的代码已有历史合入记录；Issue 保持打开，直到对应物理设备、网络或正式签名验收完成并由维护者关闭。
- P1：#5 签名链、#6 图片兼容和 #7 CLI sharp 安全升级已有对应主机或模拟器证据；物理真机与正式分发边界仍保留。#8 至 #15 继续覆盖脚本注入、并发、渲染与数据完整性。
- P2/P3：#16 至 #29 覆盖下载防护、存储、体验、CI、文档和维护清理。

## 最小验证基线

每次变更按影响范围选择验证，详情见[任务执行模板](任务执行模板.md)：

- TypeScript：`pnpm check` 与定向测试；
- Web：`pnpm --filter @hongtai/web build`，视觉改动补桌面与约 390px 截图；
- Android：相关 Gradle 单测、lint、构建；外部媒体、网络和 Media3 改动补真机证据；
- Release：签名、递增版本号、APK SHA-256 和不带降级参数的升级测试。

不要把“构建成功”“模拟器通过”或“某一个 AI 探测成功”表述为正式 release、物理真机或全部模型能力均已验证。

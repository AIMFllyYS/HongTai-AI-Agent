# 当前能力与发布状态

> 最后校对：2026-09-02。本文是能力和发布成熟度的当前来源；日期验收文档仅保存当时证据。

## 一句话结论

完整功能源码已经统一进入仓库根目录。AI 采用“一次视觉生成、最多一次纯文本格式修复、五个已校验展示板块、运行期真实深度思考”；舌象与面部观察共用分层 Prompt 和独立 Markdown 知识库。制作板块源码现有智能成片、爆款复刻与数字人单视频三种方式，已按 spec `rebuild-production-pipeline` 完成深度重构（文稿先行管线 + 阶段化 Agent 页，制作计划升 `v4`，#115/#116 并入本次范围；`v0.1.25` 发布时数字人曾改名「口播切片」）并进入公开推荐；2026-08-29 源码层又完成「一键全自动 + 数字人单视频」重定义并随 `v0.1.26` 公开分发。2026-08-30 完成**智能成片链路修复与交互统一**并随 `v0.1.27`/code35 公开分发（浏览器真实端测 16/16 断言通过）。2026-08-31 完成**后台持续运行机制（TaskGuard）**（任务契约见[后台持续运行机制](任务/2026-08-30-后台持续运行机制.md)）：长任务运行期间持有 specialUse 前台服务 + 引用计数 WakeLock，熄屏/切后台继续推进，任务终态即停；设置页新增「后台运行」区（主开关默认开启、电池优化三级引导、通知权限实测状态）；已随 `v0.1.28`/`versionCode=36` 完成 Release 构建、本地归档与公网分发（23,387,559 字节、SHA-256 `9f2a9c60…d3f5a3a`，公网文件已下载回验一致，见 [后台持续运行验收](验收/2026-08-31-后台持续运行验收.md)），`download.html` 公开推荐已切换为 `v0.1.28`，`v0.1.27` 降为历史版本；熄屏续跑等真机行为待物理设备验收。2026-08-31 完成**制作板块 UI/UX 系统性重构**（提案与遗留 Issue 见[制作页 UI/UX 重构提案](任务/2026-08-31-制作页UIUX重构提案.md)）：阶段化长页改为可点击五步步骤导航（完成一步点亮一步、已完成阶段可点回修改、未解锁步骤给原因）、新建默认直进智能成片表单 + 顶部「智能成片/爆款复刻」分段切换、生成中逐句点亮不再暴露原始流式文本、深度思考默认折叠且宽度对齐、删除项目收进页头「更多」菜单、贴纸默认右上角安全区、制作页触控目标 ≥48px；390px 与桌面截图端测验收通过（`output/browser-e2e/uiux-after/acceptance.md`）。源码推进为 `v0.1.29`/`versionCode=37` 并完成 Release 构建、本地归档与公网分发（23,387,907 字节、SHA-256 `261b85ab…cac71ef`），`download.html` 公开推荐已切换为 `v0.1.29`，`v0.1.28` 降为历史版本。Android Media3 只执行通过校验的计划；模拟器 Media3 证据见 2026-08-20 批次 11，物理真机制作主链仍待用户端测，不能表述为真机通过。项目只使用根项目目录和 Release APK 交付路径，版本唯一来源是 `android/app/build.gradle.kts`；历史版本分别归档不覆盖。当前源码为 `v0.1.36`/`versionCode=44`（B 站解析 WBI 签名修复 + 合并 B 帧 Media3 Remux 修复，见 [解析任务契约](任务/2026-09-01-B站解析WBI签名修复.md) 与 [合并任务契约](任务/2026-09-02-B站合并B帧Media3Remux修复.md)），已完成 Release 签名构建、本地归档、模拟器（API 35）真网全链路端测与公网分发；`download.html` 公开推荐已切换为 `v0.1.36`（公网文件下载回验 SHA-256 与本地归档一致），`v0.1.35`/`v0.1.34` 降为历史版本。

## 运行时能力矩阵

以下能力来自 `packages/capacitor-runtime/src/standalone-app-runtime.ts` 的 `FeatureCapabilityRegistry`。`available` 表示运行时已接入，不等同于所有设备、所有网络或正式分发均已验收。

| 能力 | 状态 | 用户可做的事 | 重要边界 |
| --- | --- | --- | --- |
| 本地档案 | available | 编辑显示名、门店信息与私有头像 | 无登录、无云同步 |
| AI 设置 | available | 选择小米 MiMo/阶跃星辰并只填写 API Key，或使用高级 OpenAI 兼容配置；分别探测文本、视觉、ASR、视频配音 | Key 只进入 Android Keystore；页面不读取原始响应或音频 |
| URL 采集 | available | 处理公开单条作品并显示七阶段真实事件；终态任务可明确删除 | 不处理 Cookie、登录、批量采集或绕过风控；运行中任务不可删除。B 站解析不再为 parse 拉整页 HTML；`-352` 映射为 `PLATFORM_RISK_CONTROLLED`，HTTP `412` 映射为可重试的 `PLATFORM_API_RATE_LIMITED`。2026-08-18 在浏览器 Vite 开发态用公开稿 `https://www.bilibili.com/video/BV1bybQ6KEQP`（54 秒）跑通采集：任务 `succeeded`、`speechStatus=transcribed`、`issues=[]`。2026-08-28 修复 CLI 采集链路回归（`.part` 临时文件导致 ffmpeg 无法推断输出格式，merge/extractAudio 失败，见 [B 站采集链路修复任务契约](任务/2026-08-28-B站采集链路修复.md)）后，CLI 用公开短链 `https://b23.tv/QuHScxo` 实跑全链路 `succeeded`/`transcribed`，产物经 ffprobe 确认 h264+aac 双轨。以上均不是 APK/真机证据。APK 端 B 站链路的真网复验已于 2026-09-02 在模拟器完成（见本行末段），物理真机复验仍待用户执行；音频槽 ISO BMFF MIME 修复自 v0.1.19+ 在包内并已被本次端测覆盖，见 [#122](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/122)；超过默认时长上限的长稿仍会失败。2026-09-01 修复 B 站解析真实根因：未签名 `x/web-interface/view` 已被 B 站风控（HTTP 412），适配器改用 WBI 签名的 `x/web-interface/wbi/view`（密钥取自匿名 `nav`，无 Cookie/登录），见 [B 站解析 WBI 签名修复任务契约](任务/2026-09-01-B站解析WBI签名修复.md)；同 IP 风控态下 CLI 对用户短链 `https://b23.tv/dRtdz4d` 实跑全链路 `succeeded`，产物 ffprobe 确认 h264+aac 双轨。该修复随 v0.1.35 (versionCode 43) 进包。2026-09-02 修复合并层第二根因：B 站视频为 H.264 High 带 B 帧（`has_b_frames=3`，decode 序 PTS 非单调），框架 MediaMuxer 无法写 ctts 且旧单调性校验直接拒绝；remux 改用 Media3 Mp4Muxer 1.10.1，每轨预扫最小 PTS 并重基到 0、AAC 轨统一 sync flag、回读校验契约同步更新，见 [B 站合并 B 帧 Media3 Remux 修复任务契约](任务/2026-09-02-B站合并B帧Media3Remux修复.md)；模拟器（hongtai-api35，API 35）v0.1.36 真网端测七阶段全绿，合并产物 1022 帧视频 + 1466 帧音频与 CDN 源逐样本一致、全程解码零错误。合并修复随 v0.1.36 (versionCode 44) 进包，APK 物理真机复验仍待用户执行 |
| 内容拆解 | available | 基于真实转写或图文证据运行 `content-analysis.v1`；带清晰口播的本地 MP4 复用同一七阶段、语音识别与正式拆解 | 公网任务仍由用户确认后拆解；本地视频选择后自动拆解。没有可用口播时明确提示更换视频，不再继续进入文字拆解；当前不分析静音视频画面，也不伪造证据 |
| 图片观察 | available | 选择舌象或面部单张图片、生成观察报告并追问；确认后进入「AI 正在观察」进行中页，完成后同一地址展示报告；最近观察支持长按，终态记录可二次确认后删除 | 页名「AI 智能诊断」；进行中页无体征数字或健康评分；先描述可见证据，再给需医院核实的不确定初步判断；不是确诊、器官功能判断、处方、概率或健康评分；`pending/running` 观察由 `deleteSession` 再次保护 |
| 存储管理 | available | 从设置「数据」区进入存储管理页：设备存储总览卡（应用占用与设备容量占比）、六区占用分布图例；缓存一键清理（操作 Sheet）、应用配置只读分组构成与报告导出（系统分享），任务/舌诊面诊/制作/模板四区进入二级页逐项二次确认删除可清理文件（舌诊面诊按舌诊/面诊分组） | 路径和原生 URI 不出端（确认弹层只展示脱敏相对路径「应用私有目录/<相对段>」）；任务、报告、计划、模板和配置数据文件始终标记为不可删除；应用配置区不提供逐项明细；Android Keystore 不提供可枚举文件大小 |
| AI 运行反馈 | available | 在内容拆解、面部观察、舌象观察期间展开查看实时“深度思考”，并依次看到五个固定展示板块；诊察思考被要求聚焦图片证据、知识组合与拍摄干扰 | 深度思考只存在于当前运行内存，不保存、不写日志、不进入追问；页面不读取供应商响应对象、不展示半截 JSON，正式报告仍以完整 Schema 校验和原子保存为准 |
| 本地视频制作 | available | 制作首页选智能成片或爆款复刻；两者创建项目后默认**一键全自动**（「一键制作成片」：分镜脚本 → 逐句配音 → 组装 → 本机合成自动推进，分步编辑保留为逃生口）。智能成片可基于成功拆解导入图片/视频素材，可填写顶部主文字或交由 AI 生成并选三种文字预设；配音优先用 AI 连接中的云端旁白，旧连接未配云端 TTS 才回退系统语音。数字人是智能成片内的二次选项：只上传**一段**预处理数字人视频，脚本、配音、全量字幕与画面裁剪拼接全部自动完成（源视频短于配音时按窗口回绕拼接并如实提示源偏短）；旧「逐字稿 + 原声切片」仅存量项目保留。爆款复刻选一条成功拆解后进入 `/replica/:taskId` 向导，另有数字人单视频路径。可删除素材、成片或整个项目 | 爆款原文与拆解只在规划请求内作为受限参考，不持久化原文；原创性校验发生在脚本生成期，配音完成后不再被合成期原创性阻断。单镜 300ms 下限与总时长 15–60 秒是**软违规**提示，不阻塞合成；硬违规（非法时长、镜头数越界、数字人窗口不守恒）仍拒绝。字幕 cue 边界优先来自实测时间戳（`asr_word`/`tts_duration`），缺词级证据时按字数比例铺排；每句字幕最多 2 个强调词由 AI 自动配置并在字幕内放大，口播字幕逐句全量不丢字。旁白会参考画面里看得见的内容（grounding 如实记录），但没有「清单要的」与「画面有的」机器比对；界面要求逐镜核对。运行中的项目受 TaskGuard 护持可在后台续跑（见「后台运行」行）；链路真实失联或进程重建时遗留 `planning/rendering` 仍按幂等中断恢复终止；删除素材会使旧计划与成片失效，规划或渲染期间拒绝删除。导出失败已拆成可区分错误码并允许软件 H.264 回退。2026-08-18 浏览器把「成功拆解 → 添加 3 张素材 → AI 生成制作计划」跑通（当时为 v2 计划）；浏览器 `render` 故意抛 `ERR_MEDIA_ENCODER_UNAVAILABLE`，不得表述为 Media3/真机合成通过。2026-08-20 API 35 模拟器 Release instrumentation 已用真实 MP4 夹具产出 v3 蒙太奇与数字人切片成片（H.264 + AAC，显示 720×1280），详见 [批次 11 Media3 验收](验收/2026-08-20-batch11-media3.md)；物理真机与 Agent 真实生成仍未验。云端 TTS 与 OEM 媒体实现仍需另验。2026-08-28 文稿先行 v4 已随 `v0.1.25` 公开推荐发布；2026-08-29 一键全自动与数字人单视频重定义已完成源码与测试（见 [制作一键全自动与数字人重定义](任务/2026-08-29-制作一键全自动与数字人重定义.md)），端测与发版未完成 |
| 模板管理 | available | 从成功拆解复制结构，或新建、编辑、删除本地模板；模板页/拆解页长按可重命名模板；有来源任务时统一显示本地视频持久化首帧缩略图（本地上传与联网下载一致） | 模板不复制原视频；封面为读路径懒回灌的 `media/thumbnail.jpg`（原生 `captureFrame` 原子写入、JPEG 复验），投影按持久化首帧 → 本地视频 → 远端封面兜底，来源媒体已清理时诚实显示不可用；删除模板与删除来源拆解双向级联并二次确认（红色警示），本机视频默认保留、勾选后才一并删除；不做云模板市场 |
| 后台运行 | available | 制作（脚本/配音/组装/渲染）、采集、拆解、观察等长任务运行期间自动持有 specialUse 前台服务（状态栏「后台运行中」常驻通知）与引用计数 WakeLock，熄屏/切后台任务继续推进，任务终态即停止服务释放锁；设置页「运行」区提供主开关（默认开启）、电池优化豁免状态与三级引导、后台通知权限状态与请求 | 前台服务承载自 `@capawesome-team/capacitor-android-foreground-service@8.1.0`（清单声明 specialUse，不受 Android 14/15 的 6 小时超时限制），WakeLock 计数与电池优化引导为最小自写 `TaskGuardPlugin`；护持失败只降级后台存活率、绝不使任务本身失败；不承诺绝对保活——长时间熄屏静置（Deep Doze）与部分国产 ROM 激进省电仍可能暂停任务，长任务建议插电；进程被强杀后 Service 重启但编排链断，回落现有幂等中断恢复；后台运行开启时任务期间允许熄屏（不再强制亮屏），关闭则回到亮屏保持原行为；熄屏续跑/电池优化引导/通知点击回应用等真机行为待物理设备验收 |
| 发布 | planned | 无界面入口；`/publish` 已移出路由表 | 不唤起平台、不伪造发布成功 |

**制作板块深度重构（源码已落地，端测验收未完成）。** 2026-08-28 按 spec [`rebuild-production-pipeline`](../.trae/specs/rebuild-production-pipeline/spec.md)（文稿先行管线 + 阶段化 Agent 页）实施：目标态为创建表单只要求一句话需求必填（来源拆解、主文字、文字预设、贴纸折叠进高级项；拆解不再是建项目前置，而是可选增强），工作台按「需求 → 分镜文稿 → 配音 → 合成 → 成片」阶段推进（每阶段一个产物与一个主按钮，已完成阶段可回改并触发诚实重建：改句只重新配音该句，其余保持）；每镜时长改为对应句实测 TTS 音频时长，总时长 15–60 秒软边界，移除预设时长四选一；字幕 cue 边界优先来自实测时间戳（Provider 支持原生时间戳时直用，OpenAI 兼容路径用 Whisper 词级转写反查并诚实标注 `asr-fallback` 精度差异）；制作计划升级 `production-plan.v4`（旧 v3 项目只读兼容渲染，存量本地项目不失效）；「数字人」模式改名「口播切片」（语义不变：自带口播 MP4 + 逐字稿 + 时长上限校验，UI 不得暗示生成数字人形象或口型；云数字人为 Phase 2 未立项）；微调能力并入阶段页分镜卡，`/create/:projectId/edit` 退役为兼容重定向。[#115](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/115)（旁白 TTS 实测时长回填）与 [#116](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/116)（词级时间戳）已并入本次重构范围。2026-08-28 源码层全部完成：`pnpm check` 全绿（613 web + 145 runtime 测试）与 web build 通过，五阶段会话页、updateStoryboard 逐句编辑、实测时长软边界、v4 计划、口播切片改名均已实现并锁定在测试中。同日 `v0.1.25`/code33 已完成主机构建、正式签名、本地归档、公网上传回验与 `download.html` 公开推荐切换（见 [v0.1.25 Release 验收](验收/2026-08-28-v025-release-candidate.md)）；浏览器/模拟器/物理真机端测未完成。文稿先行 v4 行为已随公开推荐分发；旧 `v0.1.24` APK 上的时长先行 v3 行为（预设时长四选一、`script_estimate` 字幕、独立微调页）仅存在于历史版本。

**一键全自动与数字人单视频重定义（2026-08-29 源码已完成，未发版）。** 用户两条指令推翻 `rebuild-production-pipeline` 的两大前提后实施（任务契约见[制作一键全自动与数字人重定义](任务/2026-08-29-制作一键全自动与数字人重定义.md)）：①一键推进是默认行为——`runAutomaticPipeline` 把「脚本 → 配音 → 组装 → 渲染」串成单一管线并接入智能成片、爆款复刻（含数字人单视频路径）三入口，创建项目后点「一键制作成片」即可全链自动，分步方法保留为编辑逃生口；②数字人 = 一段预处理视频——新 avatar 项目不再要求逐字稿，`planAvatarSourceWindows` 按每句实测 TTS 时长烘焙裁剪/回绕窗口（窗口时长之和必须精确等于实测时长，组装期硬校验），源视频短于配音时回绕拼接并提示源偏短，旧「口播切片」项目仅存量保留、走 v3 原声字幕路径。配套落地：分镜生成改 `script-progress` 流式（骨架句卡 + 流水文本，与其他 AI 板块一致）；分镜句新增 AI 自动配置 `emphasisWords`（每句最多 2 词、必须逐字在句内，净化后进字幕 cue 放大）；单镜 300ms 下限与总时长边界改软违规、原创性校验移至脚本生成期（配音后不再被合成期阻断）；字幕完整性回归断言（比例路径 cue 拼接去空白 === 口播全文）与 Kotlin `parseCues` 全量消费确认。`pnpm check`、web build 与定向测试全绿；浏览器 390px 视觉、物理真机 Media3 与真 API 长跑复验由用户验收，未完成前不得声称端测通过。

AI 运行反馈是两条简单 AI Flow，不是 Agent、工具调用循环或动态编排器。诊察与内容拆解在正常路径各发出一次结构化流式请求；同一响应中的五个顶层字段或字段组完整闭合后，先通过对应 Zod 与业务语义校验，再发送 `structured-generation-progress.v1` 安全投影。完整文档无效时最多进行一次格式修复，诊察修复为纯文本且不再次发送私有图片。板块进度和推理文本都不持久化；WebView 或进程退出后不恢复半成品，用户重试会重新生成完整文档，只有完整 `diagnosis-report.v1` / `content-analysis.v1` 校验并原子保存后才成立。

任务列表、任务详情、内容拆解与观察报告在健康状态下通过按 ID 或窄变更事件自动更新。权威记录先落盘再通知页面；初始异步列表读取会回放读取期间到达的 upsert、delete 或报告终态，并拒绝更早请求的迟到结果，因此不需要固定轮询或手动刷新。只有读取或订阅失败时保留“重新读取”恢复动作。

## 发布状态

| 项目 | 当前事实 | 结论 |
| --- | --- | --- |
| 版本谱系 | 最新源码与公开推荐为 `versionCode=44`、`versionName=0.1.36`（B 站解析 WBI 签名修复 + 合并 B 帧 Media3 Remux 修复）：本地归档 `HongTai-AI-Agent-release-v0.1.36.apk` 为 23,412,599 字节、SHA-256 `f53d44d9e13a67701cf5d9f74fa822f76dce426353bad0922abe0cdafd8d7142`，同一正式 Release 证书签名，已按名归档且未覆盖历史版本；公网地址 `https://husteread.com/storage/public/HongTai-AI-Agent-release-v0.1.36.apk` 已上线（下载回验 SHA-256 与本地归档一致），`download.html` 已切换推荐；模拟器升级路径实测 v0.1.35(43)→v0.1.36(44) 正常升级成功。`v0.1.35`/code43（仅解析修复，23,412,603 字节、SHA-256 `762c49e3d57f83a892fbbc8b98f0cd3f7c44e6320beda1b29f41f584f926f461`）与 `v0.1.34`/code42（23,412,591 字节、SHA-256 `307fe0941610bfe9c64cc335a8115f7640f84fb7cd3a5971a69d74c4aa575f63`）降为历史版本，公网均可下载；`v0.1.33`/code41（23,410,228 字节、SHA-256 `c49f2ea178f756015c0439b78e93f89e84d03f363b68e663ae29b328c9e6d4b5`）与 `v0.1.32`/code40（23,410,224 字节、SHA-256 `9ced370391ef49420bfb940b66d21306c0bf6eefdad287e5a05789abe7d6797a`）等更早版本降为历史版本；历史 Release APK 仍分别保存在 `output/apk-archive/`，同版本不同字节不得覆盖 | `v0.1.36` 已完成构建、签名后验、本地归档、公网分发、下载页切换与模拟器（API 35）真网全链路端测；物理真机端测未完成 |
| 错误同名 APK | 公开的 `HongTai-AI-Agent-debug-v0.1.5.apk` 已核对为 16,491,092 字节、SHA-256 `9ffb4b8eb4ef6b67fc8c13dd5f8d49d05eac81e6b9066c5832f83cb9d44e43a7`，但包内真实身份为 `0.0.1`/3 且为 Debug 签名 | 它不是本救援源码的产物，不能升级 `v0.1.4`/11；下载页已移除其安装入口，禁止覆盖文件后沿用旧哈希或宣称正式版 |
| Release 签名 | 当前公开版本 `v0.1.29`/code37 已从仓库外签名配置在根目录重新构建；APK non-debuggable，16 KiB zipalign、v2/v3、四 ABI、包身份与证书锚点全部通过。证书 SHA-256 为 `54df122cd4f99720c613737815385e771bfaeb17715c160aed178062ab5b2fde`（与历史发布一致）；公开文件 `v0.1.29` 与本地归档哈希一致 | 正式 Release 身份连续；物理真机仍未完成 |
| 图片兼容 | minSdk 24；当前 HEAD 的单张 HEVC HEIF/HEIC native fallback 已在 API 24/25 独立 x86_64 AVD 分别通过 6/6 instrumentation。WebView 53/55 上的 v7 低版本中文终态页已复验；当前标准 Android provider 的启动基线为 Chromium 89，Huawei provider 采用独立版本基线 10，不再按不可达到值 fail-closed。API 35 WebView 124 上，v7 首页、观察页及真实 Photo Picker 的平台可解码 HEIC、PNG 正向路径通过；普通 Photo Picker 会在 URI 到达应用前过滤本轮损坏 HEIC，故不声明 API 35 UI `IMAGE_INVALID` 已复验 | [#6](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/6) 已完成原生 fallback 与 v7 分层模拟器端测；Android 7.x 同设备完整 UI 闭环、物理设备、ARM 与 OEM HEIC 仍未验证；不支持 AVIF/序列 |
| 设备证据 | code17 和 code18 在 API 35 / Android 15 / x86_64 只读模拟器分别完成同证书普通覆盖升级；code19 的 Release instrumentation 实际生成 15 秒、720×1280 显示方向、顶部主文字和底部字幕并存的 H.264/AAC MP4。code20 归档 APK 以 `adb install --no-streaming -r` 从已安装 code13 普通覆盖升级，未使用降级参数或卸载，`firstInstallTime` 保持不变。2026-08-20 同一 AVD 上先装归档 `v0.1.17`/code25，再以同样命令覆盖安装归档 `v0.1.18`/code26，未使用 `-d`，`firstInstallTime=2026-08-19 17:53:58` 保持不变；冷启动后首页中文、底栏与空任务态正常。同日 Release instrumentation 用真实竖屏 MP4 夹具产出 `production-plan.v3` cues/贴纸/浮动文字与数字人顺序切片；成片哈希与抽帧见 [批次 11 Media3 验收](验收/2026-08-20-batch11-media3.md)，覆盖升级见 [v0.1.18 候选验收](验收/2026-08-20-v018-release-candidate.md) | 这是 Android WebView、页面与模拟器 Media3 / 覆盖升级证据；物理手机的 launcher 缩放、屏幕色彩、真实触控、OEM WebView 与模型端到端仍未验证 |
| 后台返回与异常终态 | 官方 `@capacitor/app` 已接入；系统媒体选择属于受控外部 Activity，选择器正常返回时只关闭对应登记并继续原 WebView，不再触发全局恢复与任务中断。2026-08-31 起长任务运行期间由 TaskGuard（specialUse 前台服务 + 引用计数 WakeLock）护持，熄屏/切后台续跑（见能力矩阵「后台运行」行）；只有进程确实重建或执行失联时，采集、拆解、观察和制作任务才进入幂等中断恢复 | 后台续跑的可靠性以「绝大多数场景」为边界：Deep Doze 与部分国产 ROM 仍可能暂停；熄屏 5 分钟续跑、电池优化引导、通知点击回应用等真机证据待物理设备验收 |
| 相机/相册权限 | 最终 Release 清单实读为 INTERNET、ACCESS_NETWORK_STATE、WAKE_LOCK、FOREGROUND_SERVICE、FOREGROUND_SERVICE_SPECIAL_USE、POST_NOTIFICATIONS、REQUEST_IGNORE_BATTERY_OPTIMIZATIONS 和 Android 自动生成的非导出 receiver 权限；相机走外部 Intent + FileProvider 单次授权，相册走 Photo Picker/单项 URI，模拟器已验证两个系统 Activity 可打开和取消返回 | 新增 4 个权限全部为 normal 级（POST_NOTIFICATIONS 为运行时请求，用户可拒且前台服务仍运行）；不声明 `CAMERA`、`READ_MEDIA_IMAGES`、`READ_EXTERNAL_STORAGE` 或 `MANAGE_EXTERNAL_STORAGE`；未来改用 CameraX 才需独立申请 `CAMERA` |
| 链接解析诊断 | #3 的原生失败分类、安全 DTO、持久化映射和通知展示已合并并关单 | 真实断网、DNS、TLS、切网与物理机网络 E2E 仍未做，不再把已关闭的 #3 当作打开的验收 Issue |
| CLI 图片解码安全 | Node 本地图片解码已精确升级到 `sharp 0.35.3`；本版本在解码前检查 JPEG/PNG/WebP 魔数，拒绝声明 MIME 与实际格式不一致的 GIF/TIFF 等输入；页面响应和媒体下载有明确上限，Node HTTPS 目标与 Android/浏览器一致拒绝本机/私网字面量；`pnpm audit --prod` 无已知生产依赖漏洞 | Windows x64 CLI 端测与本轮边界测试已完成；Linux/macOS 未验证，APK 不依赖 sharp。[#7](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/7) 的代码缺口已在本版本落地，但 GitHub Issue 未由本任务代关闭 |
| Android 质量门禁 | `v0.1.29`/code37 已通过 `scripts/build-android-release.ps1`：Web production build、Capacitor sync、Release JVM、`lintRelease`、四 ABI `assembleRelease`、包身份、正式签名、16 KiB zipalign 与本地 SHA-256 后验。本地归档与公网文件均为 23,387,907 字节、SHA-256 `261b85ab54940864f74509ed544e78325eb2c7b6f93c2b4d3f0be69ddcac71ef`（`v0.1.28` 门禁记录见 [后台持续运行验收](验收/2026-08-31-后台持续运行验收.md)） | `v0.1.29` 已公开分发且下载页已同步；仍未连接物理设备，不能声称真机制作、真实 Provider 或物理机覆盖升级 |

## 当前修复队列

当前队列以 GitHub **open 且无 `future`** 为准。已完成批次的实现细节见对应[任务](任务/)与[验收](验收/)，不在本节重复。源码地图见[项目架构解析](项目架构解析.md)。

**不要把下面已合入的代码写成门禁已过：** 物理真机全链路、Agent 真实 `generatePlan`、B 站 APK 物理真机采集仍未验收（模拟器 API 35 真网全链路已于 2026-09-02 验收）。这些是发布成熟度边界，不是把已关闭 Issue 重新打开的理由。

### 当前（open、无 future）

- 制作板块深度重构 [`rebuild-production-pipeline`](../.trae/specs/rebuild-production-pipeline/spec.md)（文稿先行管线 + 阶段化 Agent 页）**已随 `v0.1.25` 公开推荐发布，待端测验收**：一句话需求必填表单、逐句分镜文稿、TTS 前置与实测时长回填、制作计划 v4、「口播切片」改名、微调并入分镜卡均已实现（`pnpm check` 全绿、web build 通过、Release 构建归档与公网回验通过）；浏览器/模拟器/真机端测未完成；[#115](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/115)（旁白 TTS 实测时长回填）与 [#116](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/116)（词级 `asr_word` 时间戳）已并入本次重构范围（原登记为 future），不再单独排期。
- 制作「一键全自动 + 数字人单视频」重定义已随 `v0.1.26`/code34 公开推荐发布（2026-08-29）；其端测阻断问题（一句话一键制作必报「制作项目不存在或已损坏」、数字人无上传入口、新旧项目状态混杂、删除确认不统一）已在 2026-08-30 修复并随 `v0.1.27`/code35 完成构建、归档、公网上传回验与下载页切换（见 [智能成片链路修复与交互统一验收](验收/2026-08-30-智能成片链路修复与交互统一验收.md)）：浏览器真实端测（真实 AI + 真实 MP4）两条一键路径 16/16 断言通过、console 零错误；**物理真机 Media3 与真机覆盖升级仍未验**。
- [#94](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/94) 整体前端按 `docs/Hong/新设计/hongtai-mobile.pen` **未归档**画板做页面层 1:1（当前分支 `feat/hongtai-mobile-ui-redesign`）。
- [#122](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/122) APK 上 B 站解析成功后单视频下载立即失败。两个根因均已修复并进包：解析层 WBI 签名（v0.1.35）与合并层 B 帧 Media3 remux（v0.1.36，详见 [合并任务契约](任务/2026-09-02-B站合并B帧Media3Remux修复.md)）；模拟器（API 35）真网全链路端测已通过，**物理真机复验仍待用户执行**；UI 把合并真因掩盖为「媒体下载失败」的诊断缺口仍在本 Issue 跟踪。

### 已打 `future`（问题仍在，本轮不排期）

- 制作韧性：文件层原子交换 [#118](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/118)；「清单要的」与「画面有的」比对 [#120](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/120)。
- 观察产品：舌象/面部五行全身参考 [#121](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/121)。
- 产品方向：数字人对标剪映 [#97](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/97)（云数字人 Phase 2 未立项，`rebuild-production-pipeline` 明确本次不做、不画成已接通）；Capacitor 8.x 与真实安全区 [#128](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/128)。
- 2026-08-16 转 future 的 #7（危险解码器魔数预检）与 #8（harness 安全 DOM）已在本版本实现并有边界测试；GitHub 状态不由本任务代关闭。其余 #15–#19、#21、#23–#28 仍按 future 保留。

### 已关闭批次（不要再当待办）

- #1–#6、#9–#14、#22、#29 以及批次 0–7（#30–#77）已在 2026-08-16 关闭。
- 批次 8 [#85](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/85)：可用性阻断与交互结构已关；B 站下载剩余见 #122。
- 批次 9 [#98](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/98)：Chromium 89、监听器、开屏、懒加载、键盘 inset 已关；24px 安全区兜底见 #128。
- 批次 10 [#103](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/103)：字幕模板、v3 计划、烧录、微调、复刻向导、insight、装饰与双模式入口已关；后续能力见 #115 #116 #118 #120。
- 批次 11 [#123](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/123)：制作页按钮、选择器 resume、SPA 恢复已关。模拟器 Media3 见 [批次 11 验收](验收/2026-08-20-batch11-media3.md)。当前公开推荐安装包为 `v0.1.24`/code32；`v0.1.23`/code31 已作为历史版本保留，见 [v0.1.24 Release 验收](验收/2026-08-26-v024-release-candidate.md)。

## 最小验证基线

每次变更按影响范围选择验证，详情见[任务执行模板](任务执行模板.md)：

- TypeScript：`pnpm check` 与定向测试；
- Web：`pnpm --filter @hongtai/web build`，视觉改动补桌面与约 390px 截图；
- Android：相关 Gradle 单测、lint、构建；外部媒体、网络和 Media3 改动补真机证据；
- Release：签名、递增版本号、APK SHA-256 和不带降级参数的升级测试。

不要把“构建成功”“模拟器通过”或“某一个 AI 探测成功”表述为正式 release、物理真机或全部模型能力均已验证。

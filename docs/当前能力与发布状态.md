# 当前能力与发布状态

> 最后校对：2026-08-11。本文是能力和发布成熟度的当前来源；日期验收文档仅保存当时证据。

## 一句话结论

APK 已具备本地档案、AI 设置、公开链接采集、内容拆解、图片观察与本地视频制作的运行时链路；素材库和发布仍未接入。历史 `v0.0.1`/v7 已建立受控非 Debug release 签名与主机验签链，并完成模拟器上的同证书 v3→v4、v4→v5、v5→v7 普通升级；当前源码与 Debug QA 候选为 `v0.1.1`/`versionCode=8`，已完成本轮界面、系统 TTS 与应用信息的 API 35 模拟器复验。Android 7.x 的完整 UI + fallback 同设备闭环、物理真机、`v0.1.1` 的 release 签名/升级及其他发布门禁尚未完成，**仍不可正式分发**。

## 运行时能力矩阵

以下能力来自 `packages/capacitor-runtime/src/standalone-app-runtime.ts` 的 `FeatureCapabilityRegistry`。`available` 表示运行时已接入，不等同于所有设备、所有网络或正式分发均已验收。

| 能力 | 状态 | 用户可做的事 | 重要边界 |
| --- | --- | --- | --- |
| 本地档案 | available | 编辑显示名、门店信息与私有头像 | 无登录、无云同步 |
| AI 设置 | available | 保存 OpenAI 兼容连接并分别探测文本、视觉、ASR | Key 只进入 Android Keystore |
| URL 采集 | available | 处理公开单条作品并显示七阶段真实事件 | 不处理 Cookie、登录、批量采集或绕过风控 |
| 内容拆解 | available | 基于真实转写或图文证据主动运行 `content-analysis.v1` | 不自动开始，不伪造证据 |
| 图片观察 | available | 选择舌象或面部单张图片、生成观察报告并追问 | 仅日常参考，不是医疗诊断 |
| AI 运行反馈 | available | 在内容拆解、面部观察、舌象观察期间查看真实请求阶段、已接收内容量和已识别段落 | 只展示安全摘要；最终报告仍以持久化 Schema 为准，不展示 reasoning 或原始 Provider 响应 |
| 本地视频制作 | available | 基于成功拆解导入本机素材；可选“素材剪辑 + TTS”生成中文旁白与字幕，或“数字人口播”导入带原声 MP4 并按用户口播稿生成字幕 | 素材必须通过本地类型、轨道、时长和尺寸验证；中文 TTS、编码器和 OEM 媒体实现仍需真机覆盖 |
| 素材库 | planned | 显示明确的未接入状态 | 不创建假素材或假搜索结果 |
| 发布 | planned | 显示明确的未接入状态 | 不唤起平台、不伪造发布成功 |

## 发布状态

| 项目 | 当前事实 | 结论 |
| --- | --- | --- |
| `versionCode` | 当前源码和 Debug APK 为 `8`，`versionName` 为 `0.1.1`；2026-08-11 在 API 35 x86_64 模拟器使用不带 `-d` 的 `adb install --no-streaming -r` 正常覆盖 Debug 谱系，保留既有 `firstInstallTime`。历史 v7/`0.0.1` 的同一非 Debug release 证书验签与 v5→v7 模拟器升级事实仍保留在本表与对应验收记录中 | 版本继续单调递增；本轮仅证明 Debug 谱系模拟器覆盖，`v0.1.1` 的同 release 证书升级和物理真机仍未验证 |
| Release 签名 | v7 已从仓库外 properties 构建并验签；zipalign、v2/v3、包版本、公开证书锚点、12 个目标 `.so` 和四 ABI 16 KiB LOAD alignment 均通过核对。v7 APK SHA-256 为 `85c669347ed6c9d80fcf085d20a841b2c62a5dea2ee7462501633e5a996f4a0f`，证书 SHA-256 为 `54df122cd4f99720c613737815385e771bfaeb17715c160aed178062ab5b2fde`；v6 主机事实保留在 Issue #6 验收记录中 | [#5](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/5) 的受控签名链、v3→v4、v4→v5 与 v5→v7 模拟器升级验收已建立；物理真机与正式分发门禁仍未完成 |
| 图片兼容 | minSdk 24；当前 HEAD 的单张 HEVC HEIF/HEIC native fallback 已在 API 24/25 独立 x86_64 AVD 分别通过 6/6 instrumentation。WebView 53/55 上的 v7 低版本中文终态页已复验；标准 Android provider 继续要求 Chromium 99，Huawei provider 缺少可信版本映射并 fail-closed。API 35 WebView 124 上，v7 首页、观察页及真实 Photo Picker 的平台可解码 HEIC、PNG 正向路径通过；普通 Photo Picker 会在 URI 到达应用前过滤本轮损坏 HEIC，故不声明 API 35 UI `IMAGE_INVALID` 已复验 | [#6](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/6) 已完成原生 fallback 与 v7 分层模拟器端测；Android 7.x 同设备完整 UI 闭环、物理设备、ARM 与 OEM HEIC 仍未验证；不支持 AVIF/序列 |
| 真机证据 | #1/#2 的持久状态、后台导入、picker 读取授权和丢失回调清理已合并；外部相机/相册完整生命周期尚未完成物理真机验收 | 物理机验收仍保留在 [#1](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/1)、[#2](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/2) |
| 链接解析诊断 | #3 的原生失败分类、安全 DTO、持久化映射和通知展示已合并 | 真实断网、DNS、TLS、切网与物理机网络 E2E 仍需按 [#3](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/3) 验收 |
| CLI 图片解码安全 | #7 已将 Node 本地图片解码精确升级到 `sharp 0.35.3`；Windows x64 fresh frozen install 实际加载 libvips `8.18.3`，真实 CLI 回环 HTTPS Provider/HTTP API 的方向修正、2048 限边、稳定失败与请求级超限契约通过，2026-08-10 的 `pnpm audit --prod` 为 0 个已知漏洞，原 clean 工作树 `pnpm check` 为 218/218 | Windows x64 CLI 端测与统一检查已完成，spec 与 quality 均已批准；Linux/macOS 未验证，APK 不依赖 sharp |
| Android 质量门禁 | 历史 v7 主机已完成 `pnpm check`、release JVM 测试、`lintRelease`、四 ABI native 构建、release APK 构建与签名后验；本轮 `v0.1.1` 通过 226/226 `pnpm check`、Web production build、Debug JVM、`lintDebug`、Debug 与 androidTest APK 构建。API 35 x86_64 模拟器完成冷启动、观察页、TTS 系统 Activity 与应用信息页复验，关键定向日志未见 crash、ANR、OOM 或链接错误 | lint 无 error；模拟器通过不代表物理真机或全部正式发布门禁 |

## 当前修复队列

优先级标签和完整验收条件在 GitHub Issue 中维护：

- P0：[#1 外部图片选择恢复](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/1)、[#2 图片主线程导入](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/2)、[#3 链接失败诊断](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/3)、[#4 版本升级身份](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/4) 的代码已于 2026-08-09 合入 `main`；Issue 保持打开，直到对应物理设备、网络或正式签名验收完成并由维护者关闭。
- P1：[#5 签名链](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/5) 的主机实现与模拟器同 release 证书 v3→v4、v4→v5、v5→v7 普通升级验收已于 2026-08-10 完成；物理真机与正式分发门禁仍未验证。[#6 图片兼容](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/6) 的当前 HEAD fallback 已在 API 24/25 x86_64 AVD 分别通过 native 端测，v7 低版本静态页与 API 35 WebView 124 的平台 HEIC/PNG 正向 UI 组合也已通过；损坏 HEIC 在 API 35 被普通 Photo Picker 前置过滤，Android 7.x 的 WebView ≥99 同设备完整闭环仍需物理设备补验。[#7 CLI sharp 安全升级](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/7) 的精确依赖升级、Windows x64 fresh frozen install、真实 CLI 回环 HTTPS/HTTP、2026-08-10 时点生产审计和 218/218 统一检查已通过，spec 与 quality 均已批准；[#8](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/8) 至 [#15 平台目标媒体选择](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/15) 仍覆盖脚本注入、并发、渲染与数据完整性。
- P2/P3：[#16](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/16) 至 [#29](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/29)，覆盖下载防护、存储、体验、CI、文档和维护清理。

## 最小验证基线

每次变更按影响范围选择验证，详情见[任务执行模板](任务执行模板.md)：

- TypeScript：`pnpm check` 与定向测试；
- Web：`pnpm --filter @hongtai/web build`，视觉改动补桌面与约 390px 截图；
- Android：相关 Gradle 单测、lint、构建；外部媒体、网络和 Media3 改动补真机证据；
- Release：签名、递增版本号、APK SHA-256 和不带降级参数的升级测试。

不要把“构建成功”“模拟器通过”或“某一个 AI 探测成功”表述为正式 release、物理真机或全部模型能力均已验证。

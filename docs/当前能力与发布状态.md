# 当前能力与发布状态

> 最后校对：2026-08-10。本文是能力和发布成熟度的当前来源；日期验收文档仅保存当时证据。

## 一句话结论

APK 已具备本地档案、AI 设置、公开链接采集、内容拆解、图片观察与本地视频制作的运行时链路；素材库和发布仍未接入。`v0.0.1` 已建立受控非 Debug release 签名与主机验签链，并完成模拟器上的同证书 v3→v4、v4→v5 普通升级；当前 v7 候选已完成主机构建与验签，但尚未做设备升级或端测。API 24/25 native HEIF fallback 分别通过设备 instrumentation，但 Android 7.x 的完整 UI + fallback 同设备闭环、物理真机和其他发布门禁尚未完成，**仍不可正式分发**。

## 运行时能力矩阵

以下能力来自 `packages/capacitor-runtime/src/standalone-app-runtime.ts` 的 `FeatureCapabilityRegistry`。`available` 表示运行时已接入，不等同于所有设备、所有网络或正式分发均已验收。

| 能力 | 状态 | 用户可做的事 | 重要边界 |
| --- | --- | --- | --- |
| 本地档案 | available | 编辑显示名、门店信息与私有头像 | 无登录、无云同步 |
| AI 设置 | available | 保存 OpenAI 兼容连接并分别探测文本、视觉、ASR | Key 只进入 Android Keystore |
| URL 采集 | available | 处理公开单条作品并显示七阶段真实事件 | 不处理 Cookie、登录、批量采集或绕过风控 |
| 内容拆解 | available | 基于真实转写或图文证据主动运行 `content-analysis.v1` | 不自动开始，不伪造证据 |
| 图片观察 | available | 选择舌象或面部单张图片、生成观察报告并追问 | 仅日常参考，不是医疗诊断 |
| 本地视频制作 | available | 基于成功拆解导入本机素材、生成计划并在设备合成 MP4 | 仍有生命周期、并发和存储治理风险 |
| 素材库 | planned | 显示明确的未接入状态 | 不创建假素材或假搜索结果 |
| 发布 | planned | 显示明确的未接入状态 | 不唤起平台、不伪造发布成功 |

## 发布状态

| 项目 | 当前事实 | 结论 |
| --- | --- | --- |
| `versionCode` | 当前候选为 `7`，`versionName` 为 `0.0.1`；v7 主机构建与同一非 Debug release 证书验签已通过。API 24 Google APIs read-only AVD 从 v4 普通升级到 v5、API 25 与 API 35 的 v5 冷启动均为历史端测事实；v7 尚未安装到设备 | 版本继续单调递增；v7 设备升级与物理真机仍未验证 |
| Release 签名 | v7 已从仓库外 properties 构建并验签；zipalign、v2/v3、包版本、公开证书锚点、12 个目标 `.so` 和四 ABI 16 KiB LOAD alignment 均通过核对。v7 APK SHA-256 为 `85c669347ed6c9d80fcf085d20a841b2c62a5dea2ee7462501633e5a996f4a0f`，证书 SHA-256 为 `54df122cd4f99720c613737815385e771bfaeb17715c160aed178062ab5b2fde`；v6 主机事实保留在 Issue #6 验收记录中 | [#5](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/5) 的受控签名链、v3→v4 与 v4→v5 模拟器升级验收已建立；v7 设备升级、物理真机与正式分发门禁仍未完成 |
| 图片兼容 | minSdk 24；API 24/25 的单张 HEVC HEIF/HEIC native fallback 已在独立 x86_64 AVD 分别通过 6/6 instrumentation。标准 Android WebView provider 低于 Chromium 99 时显示中文终态；Huawei provider 缺少可信版本映射，当前 fail-closed 且页面诚实说明“版本过低或 provider 尚未验证支持”，不声明支持。API 35 WebView 124 上的真实 UI/bridge/Photo Picker 已通过 baseline HEIC、损坏 HEIC 与 PNG 重试 | [#6](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/6) 的原生 fallback 与分层端测是 v5 历史证据；Android 7.x 同设备完整 UI 闭环、v7 设备复验、物理设备、ARM 与 OEM HEIC 仍未验证；不支持 AVIF/序列 |
| 真机证据 | #1/#2 的持久状态、后台导入、picker 读取授权和丢失回调清理已合并；外部相机/相册完整生命周期尚未完成物理真机验收 | 物理机验收仍保留在 [#1](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/1)、[#2](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/2) |
| 链接解析诊断 | #3 的原生失败分类、安全 DTO、持久化映射和通知展示已合并 | 真实断网、DNS、TLS、切网与物理机网络 E2E 仍需按 [#3](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/3) 验收 |
| Android 质量门禁 | 当前 v7 主机已完成 `pnpm check`、release JVM 测试、`lintRelease`、四 ABI native 构建、release APK 构建与签名后验；Debug Kotlin、JVM 与 androidTest 编译回归通过 | lint 无 error；v7 主机构建不代表设备复验、物理真机或全部正式发布门禁 |

## 当前修复队列

优先级标签和完整验收条件在 GitHub Issue 中维护：

- P0：[#1 外部图片选择恢复](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/1)、[#2 图片主线程导入](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/2)、[#3 链接失败诊断](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/3)、[#4 版本升级身份](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/4) 的代码已于 2026-08-09 合入 `main`；Issue 保持打开，直到对应物理设备、网络或正式签名验收完成并由维护者关闭。
- P1：[#5 签名链](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/5) 的主机实现与模拟器同 release 证书 v3→v4、v4→v5 普通升级验收已于 2026-08-10 完成；物理真机与正式分发门禁仍未验证。[#6 图片兼容](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/6) 的 fallback 已在 API 24/25 x86_64 AVD 分别通过 native 端测，API 35 WebView 124 的 UI 组合矩阵也已通过；Android 7.x 的 WebView ≥99 同设备完整闭环仍需物理设备补验。[#7](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/7) 至 [#15 平台目标媒体选择](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/15) 仍覆盖安全、并发、渲染与数据完整性。
- P2/P3：[#16](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/16) 至 [#29](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/29)，覆盖下载防护、存储、体验、CI、文档和维护清理。

## 最小验证基线

每次变更按影响范围选择验证，详情见[任务执行模板](任务执行模板.md)：

- TypeScript：`pnpm check` 与定向测试；
- Web：`pnpm --filter @hongtai/web build`，视觉改动补桌面与约 390px 截图；
- Android：相关 Gradle 单测、lint、构建；外部媒体、网络和 Media3 改动补真机证据；
- Release：签名、递增版本号、APK SHA-256 和不带降级参数的升级测试。

不要把“构建成功”“模拟器通过”或“某一个 AI 探测成功”表述为正式 release、物理真机或全部模型能力均已验证。

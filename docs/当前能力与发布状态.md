# 当前能力与发布状态

> 最后校对：2026-08-12。本文是能力和发布成熟度的当前来源；日期验收文档仅保存当时证据。

## 一句话结论

APK 已具备本地档案、AI 设置、公开链接采集、本地 MP4 上传自动拆解、图片观察、可编辑模板与本地视频制作管理链路；制作素材、成片、项目和终态拆解任务均可由用户明确删除。当前源码候选为 `v0.1.4` / `versionCode=11`，它在 `v0.1.3` / `versionCode=10` 的应用能力基础上叠加本轮管理功能。公开下载页仍推荐已发布的 `v0.1.3`；`v0.1.4` 的物理真机与正式发布门禁尚未完成，**不可作为正式 release 分发**。

## 运行时能力矩阵

以下能力来自 `packages/capacitor-runtime/src/standalone-app-runtime.ts` 的 `FeatureCapabilityRegistry`。`available` 表示运行时已接入，不等同于所有设备、所有网络或正式分发均已验收。

| 能力 | 状态 | 用户可做的事 | 重要边界 |
| --- | --- | --- | --- |
| 本地档案 | available | 编辑显示名、门店信息与私有头像 | 无登录、无云同步 |
| AI 设置 | available | 选择小米 MiMo/阶跃星辰并只填写 API Key，或使用高级 OpenAI 兼容配置；分别探测文本、视觉、ASR、视频配音 | Key 只进入 Android Keystore；页面不读取原始响应或音频 |
| URL 采集 | available | 处理公开单条作品并显示七阶段真实事件；终态任务可明确删除 | 不处理 Cookie、登录、批量采集或绕过风控；运行中任务不可删除 |
| 内容拆解 | available | 基于真实转写或图文证据运行 `content-analysis.v1`；本地 MP4 复用同一七阶段、ASR 与正式拆解 | 公网任务仍由用户确认后拆解；本地视频选择后自动拆解；两者都不伪造证据 |
| 图片观察 | available | 选择舌象或面部单张图片、生成观察报告并追问 | 仅日常参考，不是医疗诊断 |
| AI 运行反馈 | available | 在内容拆解、面部观察、舌象观察期间查看真实请求阶段、已接收内容量和已识别段落 | 只展示安全摘要；最终报告仍以持久化 Schema 为准，不展示 reasoning 或原始 Provider 响应 |
| 本地视频制作 | available | 基于成功拆解导入图片/视频素材，生成计划并在设备合成 MP4；可删除素材、成片或整个项目 | 删除素材会让旧计划与成片失效；规划或渲染期间拒绝删除；云端 TTS、编码器和 OEM 媒体实现仍需真机覆盖 |
| 模板管理 | available | 从成功拆解复制结构，或新建、编辑、删除本地模板 | 模板不复制原视频、供应商响应或 reasoning；不做云模板市场 |
| 发布 | planned | 显示明确的未接入状态 | 不唤起平台、不伪造发布成功 |

## 发布状态

| 项目 | 当前事实 | 结论 |
| --- | --- | --- |
| 版本谱系 | 当前源码候选为 `versionCode=11`、`versionName=0.1.4`；父版本为 `v0.1.3` / code 10。公开下载页的 `v0.1.3` Debug APK 历史 SHA-256 为 `ae536c5cf6620cb902cea8d9a63b3cddf3a7e884e26604c1c0272daaee9d0d16` | 版本名称与 Android 版本码均单调递增；`v0.1.4` Debug 候选已完成构建与模拟器端测，但不提升公开下载页 |
| Release 签名 | 历史 v7 已从仓库外 properties 构建并验签；zipalign、v2/v3、包版本、公开证书锚点、12 个目标 `.so` 和四 ABI 16 KiB LOAD alignment 均通过核对 | 受控签名链与模拟器升级历史已建立；`v0.1.4` 的物理真机与正式分发门禁仍未完成 |
| 图片兼容 | minSdk 24；单张 HEVC HEIF/HEIC native fallback 已在 API 24/25 独立 x86_64 AVD 分别通过 6/6 instrumentation；标准 WebView 基线为 Chromium 89，Huawei provider 使用独立基线 10 | Android 7.x 同设备完整 UI 闭环、物理设备、ARM 与 OEM HEIC 仍未验证；不支持 AVIF/序列 |
| 设备证据 | API 35 x86_64 模拟器已通过 `v0.1.3/code10 → v0.1.4/code11` 无降级升级、系统 MP4 选择器取消不留空任务、真实 MP4 私有导入/音频提取、稳定 `AI_NOT_CONFIGURED` 终态和页面确认删除；没有物理设备 | 外部相机/相册、本地 MP4、Media3 合成与删除闭环仍未完成物理真机验收，模拟器证据不能替代真机 |
| 链接解析诊断 | #3 的原生失败分类、安全 DTO、持久化映射和通知展示已实现 | 真实断网、DNS、TLS、切网与物理机网络 E2E 仍需按 #3 验收 |
| CLI 图片解码安全 | Node 本地图片解码已精确升级到 `sharp 0.35.3`；Windows x64 fresh frozen install、真实 CLI 回环与当时的生产审计均有历史验收记录 | Windows x64 已验证；Linux/macOS 未验证，APK 不依赖 sharp |
| Android 质量门禁 | `v0.1.4` 已通过 236/236 `pnpm check`、Web production build、Capacitor 同步、70/70 Android JVM、0-error lint、Debug 与 androidTest APK 构建；Debug APK 为 39,330,485 字节，SHA-256 `1e90709a622a804b81ef7e80ccd462f77bf5d66681a18d331b95077e841d43a9` | 当前 Debug 候选自动化与模拟器门禁通过；正式 release 签名和物理真机门禁仍未完成 |

## 当前修复队列

优先级标签和完整验收条件在 GitHub Issue 中维护：

- P0：#1 外部图片选择恢复、#2 图片主线程导入、#3 链接失败诊断、#4 版本升级身份的代码已有历史合入记录；Issue 保持打开，直到对应物理设备、网络或正式签名验收完成并由维护者关闭。
- P1：#5 签名链、#6 图片兼容和 #7 CLI sharp 安全升级已有对应主机或模拟器证据；物理真机与正式分发边界仍保留。#8 至 #15 继续覆盖脚本注入、并发、渲染与数据完整性。
- P2/P3：#16 至 #29 覆盖下载防护、存储、体验、CI、文档和维护清理。

## 最小验证基线

每次变更按影响范围选择验证，详情见[任务执行模板](任务执行模板.md)：

- TypeScript：`pnpm check` 与定向测试；
- Web：`pnpm --filter @hongtai/web build`，视觉改动补桌面与约 390px 截图；
- Android：相关 Gradle 单测、lint、构建；外部媒体、网络和 Media3 改动补真机证据；
- Release：签名、递增版本号、APK SHA-256 和不带降级参数的升级测试。

不要把“构建成功”“模拟器通过”或“某一个 AI 探测成功”表述为正式 release、物理真机或全部模型能力均已验证。

# 2026-08-21 规范文档与架构解析全量校对

## 目标

- 用户可感知的结果：活规范与 README 与当前源码一致；新增一份可通读的项目架构解析，按目录说明每个源文件的职责。旧讲解明确降为 0.1.12 历史快照。

## 允许修改

- `AGENTS.md`、`README.md`、`CHANGELOG.md`
- `docs/文档索引.md`、`docs/当前能力与发布状态.md`、`docs/架构与工程规范.md`、`docs/项目整体架构方向.md`、`docs/AI应用能力层架构.md`、`docs/前端显示板块对接清单.md`、`docs/交互信息架构规范.md`、`docs/错误码与前端通知约定.md`、`docs/版本与发布流程.md`、`docs/CLI运行与产物说明.md`、`docs/任务执行模板.md`、`docs/Android发布签名与升级指南.md`、`docs/Android旧系统HEIF兼容与依赖指南.md`、`docs/本地端测配置.md`
- 新建 `docs/项目架构解析.md`、`docs/任务/2026-08-21-规范文档与架构解析全量校对.md`
- `docs/审查/项目讲解.md` 仅加过期声明
- `packages/capacitor-runtime/README.md`、`packages/platforms/src/douyin/README.md`、`packages/platforms/src/xiaohongshu/README.md`（及校对 B 站/快手 README）

## 明确不做

- 不改业务 TypeScript、Kotlin、CSS、测试断言或 APK 版本。
- 不重写 `docs/验收/`、`docs/superpowers/`、`docs/交接/`、`docs/Hong/`、早期 CLI/Debug 记录。
- 不推送远端。

## 架构归属

- 所属层：文档。不跨越到 UI / core / ai / platforms / capacitor-runtime / Android I/O 的实现。
- 源码与测试是事实权威；活文档不得与其冲突。

## 权威状态与数据

- 关键 ID：无新业务 ID。版本仍只读 `android/app/build.gradle.kts`。
- 并发、失败、重试、进程中断：不适用。历史验收不得被改写成当前事实。

## 验收

- 定向测试：`tests/architecture-minimal-runtime.test.ts`、`tests/application-runtime.test.ts`、`tests/web-planned-capabilities.test.ts`、`tests/android-plugin-boundary.test.ts`
- 构建 / lint：文档任务以内部链接、UTF-8、`git diff --check` 为准；若工作区已有无关 TS 改动，不把它们纳入本次提交，也不把它们的失败算作本文档任务失败。
- 浏览器或真机证据：无 UI 行为变更，不做端测。
- 用户实际会看到什么：文档入口能指向完整源码地图；规范里的 `AppRuntime`、Flow、计划版本、错误码、路由与代码一致。

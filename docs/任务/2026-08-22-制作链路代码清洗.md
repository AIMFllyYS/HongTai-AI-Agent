# 2026-08-22 制作链路代码清洗

## 目标

- 用户可感知的结果：制作导入、规划、微调、合成、删除、中断恢复的行为与文案不变；代码按活文档的职责切开，后续改合成不再先读 900 行混合文件。

## 允许修改

- `docs/审查/2026-08-22-架构与代码清洗审查.md`（本文的审查结论）
- `docs/任务/2026-08-22-制作链路代码清洗.md`
- `docs/文档索引.md`、`docs/项目架构解析.md`（只改文件表与审查登记）
- `packages/core/src/production-readiness.ts`、`packages/core/src/index.ts`、`packages/core/src/application-runtime.ts`（文字预设常量）
- `packages/ai/src/flows/production/production-planning-flow.ts`
- `packages/capacitor-runtime/src/standalone-production-*.ts`
- `apps/web/src/pages/CreatePage.tsx`、`apps/web/src/pages/create-page-*.test.ts`
- `apps/web/src/features/production/production-workbench-*.ts(x)`
- `apps/web/src/components/ProductionProjectCard.tsx`、`apps/web/src/features/production/production-history-list.tsx`（import 路径）
- 根 `tests/` 里只因路径搬家而必须改的源码契约
- `android/app/.../production/ProductionRenderer.kt`、`ProductionOverlays.kt`，以及新建的静态文字 overlay 文件与其 JVM 测试（若有）
- `tests/production-plan-readiness.test.ts`

## 明确不做

- 不改 Media3 成片算法、编码器回退策略、TTS 协议。
- 不推进 #94 / #115 / #116 / #118 / #120 / #97 / #122。
- 不引入 `edit-project.v1`、时间线编辑器或第二套渲染内核。
- 不拆采集 `AndroidMediaRuntime`、观察页、`pipeline.ts`、`application-runtime.ts` 的 DTO 合同。
- 不把 Flow 的 `AI_STRUCTURED_OUTPUT_INVALID` 冒到制作页「请选素材」分支。
- 不删 Kotlin Parser 的数值/枚举防御，不删 v1/v2 只读兼容。
- 不推进 `versionName` / `versionCode`，不推送远端。
- 不暂存当前工作区里无关的 UI 改版文件。

## 架构归属

- 所属层：capacitor-runtime 组合层为主；谓词在 core；页面只搬家；Kotlin 只搬家 overlay。
- 依赖方向：UI → AppRuntime → 组合层 → ai Flow / core 规则 → Android I/O。Kotlin 不回收业务规则。

## 权威状态与数据

- 关键 ID：`projectId`。计划权威仍是校验后的 `production-plan.v3`（兼容读 v2/v1）。
- 并发：同一 `projectId` 仍 single-flight（`#exclusive`）。
- 失败/中断：`planning/rendering → failed + TASK_INTERRUPTED` 语义不变；删素材仍作废计划与成片。

## 验收

- 定向测试：capacitor-runtime production/replica 测试；`tests/production-plan-v3.test.ts`、`production-planning-flow.test.ts`、`production-plan-edit.test.ts`、`avatar-caption-plan.test.ts`、`production-plan-readiness.test.ts`；web create/production/replica 合约测试。
- 构建 / lint：`pnpm check`；动了页面路径时 `pnpm --filter @hongtai/web build`；动了 Kotlin overlay 时相关 JVM 单测。
- 浏览器或真机证据：无行为变更，不做端测，不声称 Media3/真机通过。
- 用户实际会看到什么：与清洗前相同的制作主按钮、错误码与成片流程。

## 交付说明

- 改了什么：审查文档、制作组合层拆分、工作台目录、规划前置谓词、遗留静态 overlay 搬家。
- 刻意没有做什么：合成质量、真机、字幕对齐、编辑器。
- 剩余风险或新增 Issue：无新 Issue。产品缺口仍见审查文档末节与当前能力状态。

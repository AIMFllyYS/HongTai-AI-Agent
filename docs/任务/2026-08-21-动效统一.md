# 2026-08-21 动效统一

## 目标

- 用户可感知的结果：底部选择面板可点、可拖、可滑关；加号与同类弹出共用上滑+遮罩；产品路径用骨架屏代替全屏转圈；底栏点击与二级进入各有统一缓动，且不再先等退场再转圈。

## 允许修改

- `apps/web/src/components/Overlay.tsx`（新）、`Sheet.tsx`、`PageSkeleton.tsx`（新）、`RouteTransition.tsx`、`MaterialLibraryHeaderAction.tsx`、`BottomNav.tsx`、`StatePanels.tsx`、`App.tsx`
- `apps/web/src/motion/tokens.ts`、`motion/sheet-dismiss.ts`（新）
- `apps/web/src/router.ts`、`hooks/useBrowserRoute.ts`
- 各产品页里的 `LoadingState` 调用
- `apps/web/src/styles/tokens.css`、`components.css`、`shell.css`、`pages/playbook.css`
- `apps/web/src/playbook/*` 动效标本
- `tests/web-motion-foundation.test.ts`、`web-first-load-split.test.ts`、`web-playbook-contract.test.ts`、`bottom-nav-materials.test.ts`，以及新增 overlay/skeleton 契约测试
- `docs/动效规范.md`、`docs/交互信息架构规范.md`、`docs/文档索引.md`、`docs/项目架构解析.md`、`docs/架构与工程规范.md`、`CHANGELOG.md`、`AGENTS.md`、本任务契约
- 历史 2026-08-05 动效 spec/验收仅加过期声明

## 明确不做

- 不改 Flow / Schema / Kotlin / Capacitor 组合层，不推进 `versionName` / `versionCode`。
- 不删 `BrandSplash`，不改 AI 五板块流光骨架，不把素材库改成底部 Sheet。
- 不把字幕模板选择器、分段 Tab、步进滑杆改成 Sheet。
- 不新增动画依赖。不重写 `docs/superpowers/`、`docs/验收/` 正文。
- 不声称真机通过。

## 架构归属

- 所属层：UI（`apps/web`）。
- 禁止跨越：不得把动效或骨架逻辑下放到 core / ai / platforms / Kotlin。

## 权威状态与数据

- 无新业务 ID。骨架不得假装已经读到任务、报告或缓存体积。
- 路由切换失败仍落在现有 `not-found` / `EmptyState` / `IssueNotice`；不得用动画掩盖错误。

## 验收

- 定向测试：`tests/web-motion-foundation.test.ts`、`tests/web-first-load-split.test.ts`、overlay/skeleton 契约、`tests/web-playbook-contract.test.ts`、`tests/web-compose-nav.test.ts`
- 构建 / lint：`pnpm check`、`pnpm --filter @hongtai/web build`
- 浏览器：桌面与约 390px；加号/设置 Sheet 拖关；底栏点击缓动；进任务再返回；切 lazy 页见骨架不见转圈；`prefers-reduced-motion`
- 用户实际会看到什么：弹层上滑变暗、页面骨架、切页缓动统一

## 交付说明

- 改了什么：见 CHANGELOG `[未发布]`
- 刻意没有做什么：真机振动/WebView 音效、下拉刷新、版本号
- 剩余风险：Sheet 内滚动与拖关闭、重叠过渡在极高页面上的高度跳变；需真机再看一档位移

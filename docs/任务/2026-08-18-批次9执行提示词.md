# 批次 9 执行提示词

> 定稿：2026-08-18。目标 Issue：[#98](https://github.com/AIMFllyYS/HongTai-AI-Agent/issues/98)（父）与 #99、#100、#96、#101、#102、#95。
>
> 本批与批次 8 的关键差异：**主代理改代码**；子代理只做分析、端侧验收、代码审查。禁止 worktree。同一时刻只能有一个会写文件的角色（主代理）。

## 目标与边界

用户可感知结果：Chromium 89 基线上不再因新 API 抛错；制作成功不会被监听器改写成失败；深度思考与第一板块不再贴死；键盘和底部安全区不再挡输入/底栏；首屏先减重量再补开屏。

明确不做：#94、#97、#1–#29、批次 8 剩余 #85/#86/#87/#93、P2（9:16 max-height、N+1 `analysis.get`、活文档 M9、#20）。

## 授权与禁止

- 读取仓库任何文件；修改当前 Loop 的 Issue 影响范围内文件及显然连带文件。
- 新增或修改测试；运行定向测试、`pnpm check`、`pnpm --filter @hongtai/web build`、相关 Gradle。
- 每条 Loop 通过后本地 commit。不 push、不开 PR，除非用户另说。
- 禁止 `git add .`、worktree、Debug APK、假进度、泄露密钥、放宽断言。
- 工作区未跟踪的 `docs/Hong/新设计/` 原样保留。

## Loop（每条 Issue 一轮）

```text
分析子代理（只读）
  → 主代理最小修复
  → 主代理亲自跑验证命令
  → 新的端侧验收子代理
  → 不通过则主代理返修（最多两次）
  → 新的代码审查子代理
  → 有缺陷则主代理返修后再验证；改了 UI/行为则再跑端侧
  → 无应修项则 commit，进入下一条
```

分析判定「已不成立」则跳过，不硬改。同一条端侧或审查返修两次仍不过：记录卡点，跳到下一条。只能靠真机/真网判定时记残留，不写「真机已过」。

### 分析子代理（explore，只读）

```
只读复核。不要修改任何文件。

仓库：D:/projects/Dev-Tools/HongTai-AI-Agent，当前分支。
先读 AGENTS.md 与 docs/架构与工程规范.md。
目标 Issue：#<N>。用 gh issue view <N> 读正文。
审查基线是 4f0c17b；请对当前 HEAD 复核。

按以下格式返回，全文不超过 40 行，不要粘贴代码块：
1. 结论：仍成立 / 部分成立 / 已不成立
2. 涉及文件：路径 + 行号
3. 最小改动方案：分条，每条一句
4. 影响面
5. 风险：越界 / 并发 / 生命周期 / 真实性
```

### 端侧验收子代理（只读，每轮新开）

```
只读端侧验收。不要修改任何文件。不要声称真机、APK、Provider 或 Media3 已过。

仓库：D:/projects/Dev-Tools/HongTai-AI-Agent。
Issue #<N>。验收标准以 gh issue view 为准。
在浏览器约 390px 与桌面核对该条用户可见结果。

返回：通过 / 失败；复现步骤；DOM 或截图证据；是否要返修。全文不超过 30 行。
```

### 代码审查子代理（只读，每轮新开）

```
只读代码审查。不要修改任何文件。

对照 AGENTS.md 的 Code Review Rules。
只报本次改动引入、可复现、作者应修的问题。
优先：跨层越界、并发覆盖、生命周期、密钥、真实性、数据丢失。

返回缺陷清单或「无应修项」。全文不超过 25 行。
```

## 执行顺序

1. #99 WebView 89 API
2. #100 制作 `#emit` 成功改失败
3. #96 深度思考间距
4. #101 键盘与底部 inset
5. #102 首屏拆包与字体
6. #95 品牌开屏（建议在 #102 之后）

## 各条验证

| Issue | 最低验证 |
| --- | --- |
| #99 | 定向 compat / 相关单测，`pnpm check` |
| #100 | `pnpm --filter @hongtai/capacitor-runtime test`，`pnpm check` |
| #96 | Web 定向测试，`pnpm --filter @hongtai/web build`，390px |
| #101 | 布局契约测试，Web build，清单检查；390px；不引入 dvh |
| #102 | Web build 看拆包体积，`pnpm check` |
| #95 | Web/Android 主题与启动路径，失败仍有 ErrorState；不假进度 |
